## 1. 入口契约与回归测试

- [x] 1.1 补套图记录控件测试，覆盖固定标签 `temuexcel导出工作台`、零勾选不禁用、勾选数量不再写入标签，以及生成/刷新/删除等非选择安全阻塞保持有效。
- [x] 1.2 补主入口控制器测试，覆盖零勾选和多条勾选均以空 `setIds` 调用工作台启动分支，不修改 `recordCheckedSetIds`，并仍只绑定一个点击监听。
- [x] 1.3 复用工作台启动器的既有初始化协议，验证空 `setIds` 时覆盖层默认选中工作台标签且子文档不打开导入对话框。
- [x] 1.4 复核子文档的空 `setIds` 分支不调用 Studio 导入，工作台内“从 Studio 导入”按钮仍可显式打开既有对话框。
- [x] 1.5 补批量快速导出回归测试，覆盖零选择不打开批量导出对话框、显式选择仍按现有规则进入批量流程，且不会改变主入口初始化消息。

## 2. 最小前端实现

- [x] 2.1 将套图记录主入口文案改为 `temuexcel导出工作台`，保留既有元素标识和控件同步/监听所有权。
- [x] 2.2 从主入口的可用性与点击守卫中移除“必须勾选记录”条件，保留现有非选择安全阻塞和对应反馈。
- [x] 2.3 将主入口到 `createTemuWorkbenchLauncher` 的调用固定为传入空集合；启动器继续用既有 `temu-workbench:init` 协议发送空 `setIds`。
- [x] 2.4 保持工作台子文档现有的空 `setIds` 分支，使其只同步主题/语言而不自动调用 Studio 导入对话框。
- [x] 2.5 保持工作台内手动“从 Studio 导入”流程、覆盖层关闭后 iframe 草稿保留、以及“批量快速导出”标签的既有显式选择与导出行为不变。

## 3. 验证

- [x] 3.1 运行套图记录导出控制器、工作台子文档和批量快速导出的聚焦测试。
- [x] 3.2 使用浏览器在零勾选与多勾选两种状态下验证：主入口均直达工作台主界面，不自动弹出“从 Studio 导入”；随后验证批量快速导出在显式勾选后可沿用现有流程。
- [x] 3.3 运行 `npx openspec validate direct-temu-workbench-entry --strict`、`git diff --check`，并复核本次新增中文文本为 UTF-8 且无乱码或替换字符。
