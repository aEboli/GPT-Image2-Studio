## 1. Regression coverage

- [x] 1.1 Add planner assertions for the renamed `benefit` label, hero non-dimension fact coverage and target-language boundary.
- [x] 1.2 Add planner assertions for decisive atmosphere composition, duplicate-selling-point removal, and complete functional coverage with seamless stitch fallback.
- [x] 1.3 Add lightbox layout assertions for the compact desktop inspector and image-first narrow layout.

## 2. Creation planning implementation

- [x] 2.1 Update canonical role labels, briefs, shopper questions, buyer-decision guidance, scenario guidance, and rendering constraints while preserving stable IDs.
- [x] 2.2 Add bounded hero fact aggregation that excludes exact dimension/weight facts and is translated by existing target-language guidance.
- [x] 2.3 Add functional coverage collection and stitch fallback guidance, then audit platform/category prompt injections for contradictory comparison language.
- [x] 2.4 Synchronize the changed canonical library modules into `public/lib` and update any preview role labels without changing historical IDs.
- [x] 2.5 Map the historical universal second slot to 目标人群共鸣图 in the display layer while preserving the later 卖点图 and immutable saved manifests.

## 3. Lightbox implementation

- [x] 3.1 Reduce the desktop inspector column and parameter textarea minimum height while preserving file fields and independent scrolling.
- [x] 3.2 Verify mobile/tablet media-first grid, touch targets, focus states, and no horizontal overflow.

## 4. Verification and handoff

- [x] 4.1 Run focused planner, platform-policy, and layout tests plus `npm run sync:public-lib` drift checks.
- [x] 4.2 Run the full project test suite and record unrelated baseline failures separately.
- [x] 4.3 Run strict OpenSpec validation, start an isolated local server, and capture desktop/narrow browser evidence for image dominance and readable parameters.
- [x] 4.4 Archive the change after the parent task confirms the combined Creation prompt and Lightbox diff, then confirm the delta is merged into the main specs with history retained.
