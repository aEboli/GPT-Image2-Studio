## 1. Listing 标题货号清理

- [x] 1.1 为商品名与 SKU 来源中的货号建立中英文标题失败用例，并验证内部标识元数据保持不变
- [x] 1.2 在 Listing 提示词和响应规范化中加入边界明确的货号排除逻辑
- [x] 1.3 确认 Listing Agent 与 planner 仅由服务端和 Worker 的 `lib/` 引用，无 `public/lib` 镜像需同步，并运行针对性测试

## 2. SKU 图命名

- [x] 2.1 更新 SKU 计划测试，覆盖有颜色和无颜色时的显示名、`filenameToken` 与参考关联
- [x] 2.2 修改 SKU 显示名和文件名 token 构建逻辑，使其不再采用标题、ID 或参考文件名
- [x] 2.3 验证本地服务与 Cloudflare Worker 的保存路径均优先使用安全 SKU token

## 3. 验证与维护闭环

- [x] 3.1 运行相关测试、全量测试、`git diff --check` 和中文乱码检查（全量 1605 项中 1603 通过、1 跳过，1 个既有时间线样式断言失败）
- [x] 3.2 严格验证 OpenSpec 变更，将增量规格同步回主规格并归档
