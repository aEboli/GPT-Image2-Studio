## 1. 规格与失败基线

- [x] 1.1 增加清单解析、批次选择、可信主机、下载文件名和 15 张剩余槽位的失败测试。
- [x] 1.2 增加 Local 单图代理、扩展 ZIP 端点、工具菜单和桌面打包白名单的失败测试。
- [x] 1.3 增加 1688 fixture，覆盖主图、详情图、SKU 图以及推荐、评价、头像、Logo、广告和占位图排除。

## 2. Studio 剪贴板批次导入

- [x] 2.1 实现版本化商品图清单的规范化、序列化、大小/条目限制、类别排序和剩余槽位选择逻辑，并同步 `public/lib`。
- [x] 2.2 在 Creation 参考图标题区增加“从剪贴板导入”，支持原生剪贴板图片和采集清单，继续复用现有 `applyCreationReferenceFiles()`。
- [x] 2.3 增加响应式批次预览、分组选择、选择数量边界、加载状态和已导入/重复/失败/未选择反馈。
- [x] 2.4 增加 Local 受限单图代理，验证来源、图片主机、每次重定向、内容类型、超时和 20 MiB 上限；云端返回不支持反馈。

## 3. 1688 浏览器扩展

- [x] 3.1 建立 Manifest V3 侧边栏扩展和最小权限清单，点击 action 后按需扫描当前 1688 商品页。
- [x] 3.2 实现已知商品区域提取、非商品祖先排除、懒加载/高清 URL 选择、尺寸过滤、分组和去重。
- [x] 3.3 实现分组缩略图、全选/反选、重新采集、已选计数、空状态和错误状态。
- [x] 3.4 实现复制 Studio 清单、同一商品文件夹批量下载、确定性文件名、冲突处理和初始 `manifest.json`（后由 8.3 按新需求移除 JSON 下载）。
- [x] 3.5 增加中文 README/安装说明并复查扩展中文内容编码。

## 4. 插件分发接入

- [x] 4.1 实现可测试的扩展 ZIP 构建模块和 `build:extension` 命令，把共享协议精确复制进包内。
- [x] 4.2 增加 Local 扩展 ZIP 下载端点和创作工具菜单“商品图采集插件”入口，反馈只说明下载完成。
- [x] 4.3 将扩展源码加入 Electron Builder 文件白名单，确认云构建和旧分发模式不执行本地打包逻辑。

## 5. 验证与交付

- [x] 5.1 运行商品图采集聚焦测试、`npm run sync:public-lib -- --check`、`npm run build:extension` 和 `npm run build:pages`。
- [x] 5.2 运行 `npm test`、`git diff --check` 和中文乱码检查，修复仅由本 change 引入的问题。
- [x] 5.3 启动真实本地应用，在桌面和 `390×844` 视口验证工具菜单、剪贴板按钮、批次弹层、15 张边界、无重叠及可关闭行为。
- [ ] 5.4 使用代表性 1688 fixture 和至少一个可访问的真实 1688 页面验证分组、非商品排除、复制清单和同目录下载；无法访问真实页面时明确记录该验收缺口。
  - 已在真实页面验证 11 张主图、6 张 SKU 图、非商品排除，并通过真实 CDN 清单向 Studio 导入 15 张；Chrome 自动化安全策略禁止访问扩展管理页，加载扩展后的侧边栏复制与同目录下载仍需人工验收。
- [x] 5.5 执行 `openspec validate add-product-image-collector --strict --no-interactive` 和全项目 OpenSpec 校验，更新任务状态并报告未归档的既有 changes。

## 6. 真实页识别修复与悬浮窗

- [x] 6.1 增加 action 直接使用点击标签页、消息使用 `sender.tab`、不再请求 `sidePanel` 权限和悬浮窗贴边隐藏的失败测试。
- [x] 6.2 将侧边栏替换为按需注入的 Shadow DOM 悬浮窗，保留采集、分组选择、复制清单和同目录下载。
- [x] 6.3 支持悬浮窗拖动、左右贴边自动隐藏、把手展开、离开后收起和关闭清理，并更新中文安装说明与扩展版本。
- [x] 6.4 在截图对应的真实 1688 页面复测商品图数量，构建新 ZIP，并运行聚焦、全量、OpenSpec 和编码检查。
  - 截图页面直接采集得到 9 张主图和 4 张 SKU 图；`v1.0.1` 构建与全部自动检查通过。Chrome 扩展管理页不允许自动控制，当前安装实例重载后的悬浮窗、复制和下载仍纳入 5.4 人工验收缺口。

## 7. 结构化商品数据识别优化

- [x] 7.1 增加真实页面结构 fixture 和失败测试，覆盖 5 张声明主图、10 张未挂载详情图、7 个 SKU 规格映射到 2 张独立图片，以及详情请求失败降级。
- [x] 7.2 优先解析页面声明的 `mainImage`、`skuProps` 和受限 `detailUrl`，保留已知区域 DOM 回退并继续排除非商品图。
- [x] 7.3 在悬浮窗和 Studio 清单中保留 SKU 规格名称，明确显示独立图片数与覆盖规格数，并更新扩展说明和版本。
- [ ] 7.4 构建新 ZIP，运行聚焦、全量、OpenSpec、同步和编码检查，并在截图对应真实页面复测分组结果。
  - `v1.0.2` ZIP、27 项商品图测试、1384 项全量测试、OpenSpec、同步和编码检查均已通过；真实页面声明数据与公开详情响应确认应为 5 张主图、10 张详情图、2 张独立 SKU 图并覆盖 7 个规格。当前 Chrome 页面仍运行无版本标记的旧悬浮窗，重载安装实例后的最终可见分组验收尚未完成。

## 8. 常驻入口与下载命名优化

- [x] 8.1 增加受限悬浮入口、旧版本替换、打开/关闭联动和精确内容脚本边界测试。
- [x] 8.2 增加只选主图、只选详情图、只选 SKU 图以及面板中文名称展示。
- [x] 8.3 将图片命名改为主图/详情图/SKU 规格格式，下载目录增加本地日期 `YYMMDD` 父目录，并移除 JSON 下载。
- [x] 8.4 更新安装说明与 `v1.0.3` 包名，运行聚焦、全量、构建、同步、OpenSpec 和编码检查。
  - `v1.0.3` ZIP、29 项商品图测试、1386 项全量测试、20 项全项目 OpenSpec、public/lib 同步、Pages 构建、差异格式和乱码检查均已通过。
- [ ] 8.5 重载实际扩展，在真实 1688 商品页验证悬浮入口、面板联动、分类选择、下载文件名、日期目录和无 JSON 下载。
  - 当前真实商品页检测到的安装实例仍为面板 `v1.0.2` 且没有 `v1.0.3` 悬浮入口；Chrome 安全边界要求用户在扩展管理页手动重新加载后再刷新商品页，自动化未绕过该确认。

## 9. 粘贴、缩略图与插件预览优化

- [x] 9.1 增加失败测试，覆盖套图界面粘贴清单自动打开、同源受限缩略图、结构化详情成功时不追加 DOM 备用图。
- [x] 9.2 实现 Studio 清单粘贴处理和懒加载真实缩略图，保持普通文本、原生图片粘贴和 15 张边界不变。
- [x] 9.3 为扩展卡片增加查看和单图下载按钮，实现悬浮窗内查看器、滚轮缩放及关闭恢复。
- [x] 9.4 将扩展 action 的重复注入改为只展开现有面板，更新安装说明和 `v1.0.4` 包名。
- [x] 9.5 运行商品图聚焦测试、同步、扩展与 Pages 构建、全量测试、OpenSpec 严格校验、差异与中文编码检查。
  - `v1.0.4` ZIP、38 项商品图聚焦测试、1439 项全量测试、23 项全项目 OpenSpec、86 个 public/lib 同步模块、Pages 构建、差异格式和中文编码检查均已通过。
- [ ] 9.6 重载实际扩展，在真实 1688 商品页验证直接展开、准确数量、卡片查看/缩放/单图下载，并在真实 Studio 验证 `Ctrl+V` 和缩略图。
  - 真实 1688 页面已确认 17 张独立图（主图 5、详情图 10、SKU 图 2）；隔离加载的 `v1.0.4` 已验证 17 组查看/下载按钮、悬浮窗内查看、滚轮从 100% 放大到 115% 且页面不滚动、单图下载反馈，以及贴边收起后重复注入直接展开。真实 Studio 已验证 `Ctrl+V` 自动打开 17 张候选、17 张缩略图全部加载，并按 15 张上限完成 15/15 导入。当前实际安装实例已为 `v1.0.4`，但页面悬浮入口首次点击实测返回缺少站点权限错误；该问题由 10.2 的 `v1.0.5` 修复，仍需在扩展管理页手动重载后完成最终验收。

## 10. 全高面板、入口首击与查看器导航

- [x] 10.1 增加失败测试，覆盖受限 `detail.1688.com` 站点权限、右侧停靠全高、纯图标卡片操作、查看器名称/次序和前后切换。
- [x] 10.2 修复页面悬浮入口因缺少站点权限而必须先点击扩展 action 的问题，保持自动内容脚本和运行时路径校验仅允许 `/offer/*`。
- [x] 10.3 将默认停靠面板改为网页可视区右侧全高，在查看器底部增加图片名称、整体次序和前后切换，并把查看/下载改为带提示的纯图标按钮。
- [x] 10.4 更新中文安装说明和 `v1.0.5` 包名，运行聚焦、同步、扩展与 Pages 构建、全量测试、OpenSpec、差异和编码检查。
  - `v1.0.5` ZIP、38 项商品图聚焦测试、1439 项全量测试、23 项全项目 OpenSpec、86 个 public/lib 同步模块、Pages 构建、差异格式和 14 个本次文件的中文编码检查均已通过。
- [ ] 10.5 重载实际扩展，在真实 1688 商品页验证悬浮入口首次点击、右侧全高、图标按钮、查看器前后切换和下载反馈。
  - 真实商品页隔离加载 `v1.0.5` 后确认 17 张卡片、34 个无可见文字的查看/下载图标按钮；桌面视口中面板为 420×876 且上下边界与 876px 视口一致，390×844 窄视口中面板同样覆盖完整可视高度。查看器已验证首张/末张边界、图片名称和整体次序、前后切换、115% 切图后重置为 100% 及单图下载反馈。当前实际安装实例仍为 `v1.0.4`，需要用户手动重载 `v1.0.5` 后验证首次点击权限修复。

## 11. Studio 批次选择与图片放大

- [x] 11.1 增加失败测试，覆盖全选、反选、主图/详情图/SKU 分类选择、剩余槽位限制、单行工具栏和卡片下方纯图标放大入口。
- [x] 11.2 实现批量选择命令和弹窗内大图查看，保证放大操作不触发勾选且关闭后保留选择状态。
- [x] 11.3 运行商品图聚焦测试、public/lib 同步、Pages 构建、全量测试、OpenSpec、差异和中文编码检查。
  - 33 项商品图聚焦测试、1447 项全量测试、87 个 public/lib 同步模块、Pages 构建、24 项全项目 OpenSpec、差异格式和本次中文编码检查均通过。
- [x] 11.4 在桌面及 `390×844` 视口验证工具栏不换行、窄屏横向滚动、分类选择容量边界以及大图打开/关闭。
  - 桌面视口确认 17 张真实缩略图全部加载，反选/主图/详情图/SKU/全选分别得到 2/5/10/2/15 张；`390×844` 视口中弹窗为 372×826，工具栏 371px 可视宽度对应 558px 内容宽度且五个命令保持同一行。桌面与移动端均确认 800×800 原图在当前弹窗内放大，关闭后仍保留 15 张选择并恢复到原放大按钮。

## 12. 显式自动隐藏与拖动修复

- [x] 12.1 增加失败测试，覆盖移除贴边白色把手、自动隐藏与全选/反选同一行、窗口级指针拖动和按钮区域不触发拖动。
- [x] 12.2 实现保留状态的显式自动隐藏与悬浮入口恢复，并修复从全高停靠状态拖离后的持续移动和稳定浮动位置。
- [x] 12.3 更新中文安装说明与 `v1.0.6` 包名，运行商品图聚焦测试、同步、扩展与 Pages 构建、全量测试、OpenSpec、差异和编码检查。
  - `v1.0.6` ZIP、18 项商品图聚焦测试、1448 项全量测试、87 个 public/lib 同步模块、Pages 构建、23 项全项目 OpenSpec、差异格式和本次中文编码检查均已通过。
- [ ] 12.4 在真实 1688 商品页验证白色把手已移除、自动隐藏/入口恢复、标题栏拖动、边缘停靠和按钮不误触拖动。
  - ChatGPT Chrome 扩展和本机连接配置检查正常，但验收时 Chrome 未运行；按浏览器安全边界需用户确认启动 Chrome 后继续，当前不把源码断言或构建结果记录为真实页面验收。

## 13. 单 SKU、详情顺序、靠边折叠与网页叠加查看

- [x] 13.1 增加失败测试，覆盖跨分类共用图片的单 SKU、`__r__` 详情重复项、首次出现顺序、500px 面板、相邻加粗分类计数、折叠状态文案和网页叠加查看手势。
- [x] 13.2 将图片去重改为分类内执行并规范化详情缓存参数，保证每组按来源首次出现顺序独立连续编号。
- [x] 13.3 实现“开启折叠 / 关闭折叠”的靠边收缩模式、约 500px 面板和更醒目的分类标题/数量。
- [x] 13.4 将图片查看改为保留并压暗背景的全网页叠加层，左上角显示“返回”且不显示右上角关闭叉号，支持空白关闭、滚轮缩放、左键拖动、快速双击关闭和前后切换。
- [x] 13.5 更新中文安装说明与 `v1.0.7` 包名，运行商品图聚焦测试、同步、扩展与 Pages 构建、全量测试、OpenSpec、差异和编码检查。
  - 34 项商品图聚焦测试、1450 项全量测试、87 个 public/lib 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec、相关差异格式和中文 UTF-8 检查均通过；设计 QA 最终结果为 passed。
- [ ] 13.6 重载实际扩展，在真实 1688 商品页验证 5 张主图、9 张有序详情图、1 张白色 SKU 图、折叠恢复、面板宽度及查看层全部交互。
  - 已使用真实商品页数据和真实图片在 Chrome 隔离加载当前 `v1.0.7` 源码，确认 5/9/1、15/15 缩略图、500px 面板、47.7px 折叠边缘、选择保持和全部查看手势。实际商品页当前没有注入面板或悬浮入口；`chrome://extensions` 内部页不能由当前控制会话接管，因此仍需人工加载或重载构建后的扩展再完成安装态验收。

## 14. 完整分组规范化与四列宽面板

- [x] 14.1 增加失败测试，覆盖 5 张主图、7 张详情图和 2 张 SKU 图跨分类复用 URL 时仍完整保留，以及 900px、175×175px、四列和同色相同饱和度明度分层。
- [x] 14.2 将清单规范化的 URL 去重范围改为分类内，保持同分类规范化去重、跨分类角色、规格标签和来源顺序。
- [x] 14.3 实现 900px 面板、桌面 4 个 175px 固定列、175×175px 图片媒体区和三档等色相等饱和度分组背景。
- [x] 14.4 更新中文安装说明与 `v1.0.8` 包名，运行商品图聚焦测试、同步、扩展与 Pages 构建、全量测试、OpenSpec、差异和编码检查。
  - `v1.0.8` ZIP、21 项商品图聚焦测试、1451 项全量测试、87 个 public/lib 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec、相关差异格式和中文 UTF-8 检查均通过。
- [x] 14.5 在目标 1688 商品页验证直接采集为 5/7/2、SKU 覆盖 3 个规格，并检查面板尺寸、每行数量、缩略图尺寸和分组明度层次。
  - 真实页面当前源码隔离验收得到 5/7/2、14/14 缩略图和 3 个 SKU 规格；`1707×876` 视口下面板为 `900×876`，首行 4 张，媒体区为 `175×175`，三组背景计算色按明度连续变化。验收后已恢复页面原状态。
- [ ] 14.6 在 `chrome://extensions` 手动重载解压目录中的 `v1.0.8`，刷新目标商品页并确认安装态结果与 14.5 一致。
  - 当前安装实例仍为 `v1.0.7`；浏览器控制安全边界不允许接管扩展管理内部页，因此不把隔离加载记录为安装态重载完成。

## 15. 五分之二宽度与六按钮单行

- [x] 15.1 增加失败测试，覆盖 620–760px 受限的 38vw 面板、四列最大 160px 的自适应缩略图和六个按钮单行不换行。
- [x] 15.2 将面板宽度改为约桌面视口 38%，把四列卡片改为不超过 160px、通常约 150px 或更小的等比自适应布局。
- [x] 15.3 将全选、反选、折叠和三个分类选择按钮合并为同一行，并为窄屏保留单行横向滚动。
- [x] 15.4 更新中文说明与 `v1.0.9` 包名，运行聚焦、全量、同步、构建、OpenSpec、差异和编码检查。
  - `v1.0.9` 的 13 项聚焦测试、1451 项全量测试、87 个 public/lib 同步模块、扩展与 Pages 构建及当前 change 严格校验均通过。
- [x] 15.5 在进入真实页验收时收到追加截图，确认最终目标改为插件内查看和无留白相邻网格；不把 `v1.0.9` 记录为最终可视验收，后续由 16.5 取代。

## 16. 插件内查看与无留白相邻网格

- [x] 16.1 增加失败测试，覆盖 620–640px 面板、四列无左右留白和无间距相邻卡片，以及查看层完全包含在插件面板内。
- [x] 16.2 将查看层移入采集面板，限制遮罩、缩放舞台、导航和图片都不超出插件边界，同时保持返回、滚轮、拖动、双击和前后切换交互。
- [x] 16.3 将网格改为四个无间距等宽列，移除分组左右内边距和卡片圆角，并把面板最大宽度收紧到 640px，使桌面缩略图约为 150–160px。
- [x] 16.4 更新中文说明与 `v1.0.10` 包名，运行聚焦、全量、同步、构建、OpenSpec、差异和编码检查。
- [x] 16.5 在真实 1688 页面隔离加载 `v1.0.10`，测量面板宽度、四列尺寸和边缘间距，并验证放大层不超出插件。
  - `1707×876` 真实页面隔离验收中，面板为 `640×876`，首行四张媒体宽度均约 `157.17px`，四列无间距；查看层父级为面板且使用绝对定位，115% 缩放后仍被插件边界裁切，商品页其余 `1052px` 保持可见。

## 17. 参考界面的分组操作与卡片层次

- [x] 17.1 增加失败测试，覆盖 680–700px 面板、四列 10px 间距、蓝色选中边框、加大图片名称、分组已选数、组内全选半选态、组内下载和六控件查看工具栏。
- [x] 17.2 实现分组标题栏、只影响本组的全选与反选状态同步，以及只下载本组已选图片的纯图标按钮。
- [x] 17.3 调整面板宽度、分组内边距、卡片间距、边框和图片名称层次，并实现适应插件、左右旋转、放大、缩小和 1:1 原始尺寸工具栏，保持六个总控按钮单行和插件内查看器边界。
- [x] 17.4 更新中文说明与 `v1.0.11` 包名，运行聚焦、全量、同步、构建、OpenSpec、差异和编码检查。
- [x] 17.5 在真实 1688 页面隔离加载 `v1.0.11`，验证四列尺寸、分组操作、组内下载和插件内查看，并完成同视口参考图对照与设计 QA。
  - `1707×876` 真实页面验收中，面板为 `682.9×876px`，四列约 `152.4px`、媒体区约 `148.4px`、间距 `10px`；34/34 张真实缩略图加载且无破图。组内半选、恢复全选及只提交 4 张主图的组内下载均通过。六控件查看器完成旋转、滚轮、1:1、拖动、双击关闭和面板边界检查；`design-qa.md` 最终结果为 passed。

## 18. 多平台商品详情页适配

- [x] 18.1 增加 Amazon、Temu、TikTok Shop、SHEIN 和大健云仓的详情页 URL、图片 CDN、分组顺序、推荐/评价排除和跨平台域名拒绝测试。
- [x] 18.2 将采集器重构为平台适配器，共享 URL 规范化、图片过滤、分类内去重、连续编号和文件命名，保留 1688 结构化详情读取行为。
- [x] 18.3 扩展共享清单与本地代理信任模型，按来源平台验证图片与每次重定向，并同步 `public/lib`。
- [x] 18.4 更新 Manifest 站点权限、后台与悬浮入口完整 URL 校验、中文说明、共享打包文件和 `v1.1.0` 包名。
- [x] 18.5 运行商品图聚焦测试、public/lib 同步、扩展与 Pages 构建、全量测试、OpenSpec 严格校验、差异和中文编码检查。
  - `v1.1.0` 的 40 项商品图聚焦测试、1456 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验、相关差异格式和中文 UTF-8 检查均通过；扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.0.zip`。
- [x] 18.6 在每个平台至少一个可访问的真实消费者商品详情页验证入口、分类和缩略图；站点登录、风险验证或本机拦截导致无法验收时记录具体缺口，不绕过平台保护。
  - Amazon 真实商品 `B008N4NIBS` 已用当前采集器识别为 Amazon，得到 7 张主图、6 张详情图并排除视频缩略图；其中一张 Amazon JPEG 已通过本地代理成功获取，响应为 151636 字节。
  - Temu 当前可检索商品链接重定向至售罄页，无法完成在售详情页分类验收；Temu AVIF 与原始 PNG CDN 图片均已通过来源平台配对校验和本地代理实测。
  - TikTok Shop 在本机浏览器导航时被客户端策略以 `ERR_BLOCKED_BY_CLIENT` 拦截；SHEIN 重定向至风险验证/CAPTCHA；大健云仓已确认官方 GigaB2B 市场及 `product/product&product_id=` 详情路由，但商品详情需要登录或验证码。上述限制均未绕过。
  - 1688 真实商品页可见，但 Chrome 对文档响应报告暂停，无法在该页执行当前采集器；现有真实结构 fixture 与全部 1688 回归测试通过。其余五个平台均以代表性 fixture 覆盖入口、分类、顺序、缩略图、非商品排除和跨平台域名拒绝。

## 19. 1688 同图多规格完整保留

- [x] 19.1 更新 proposal、design 和增量规格，明确主图/详情图继续按 URL 去重，而同一图片对应的不同 SKU 规格必须分别保留。
- [x] 19.2 增加“大号、小号共用图片但第三个规格使用另一图片”的失败测试，覆盖采集结果、清单规范化、连续编号和规格文件名。
  - 修复前聚焦运行 24 项测试，其中 4 项按预期失败：1688 声明数据仍把 7 个规格压缩为 2 张图，截图场景仍把 3 个规格压缩为 2 张图，详情接口失败降级仍只有 2 张 SKU，清单规范化也把 3 个条目压缩为 2 个。
- [x] 19.3 修改 1688 声明 SKU、DOM 回退和共享清单的去重身份，确保同 URL 不同规格可独立显示、选择、复制和下载。
- [x] 19.4 更新中文说明、悬浮窗状态文案和 `v1.1.1` 包名，运行聚焦、全量、同步、构建、OpenSpec、差异和编码检查。
  - `v1.1.1` 的 42 项商品图聚焦测试、1458 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验、相关差异格式和 20 个本次文件的中文 UTF-8 检查均通过。
- [ ] 19.5 重载实际扩展并在截图商品页确认 SKU 图从 2 张变为 3 张，名称分别对应大号、小号和第三个规格；若浏览器扩展管理安全边界阻止自动重载，则保留人工验收缺口。
  - 已在截图商品 `975637799310` 的真实页面隔离运行当前 `v1.1.1` 采集器，结果为 5 张主图、3 张 SKU 图；SKU 依次为小号、大号、塔器颜色联系客服，前两项 URL 完全相同但分别保留 `sku-1`、`sku-2` 及独立文件名，`skuVariantCount` 为 3。当前 Chrome 安装实例的面板与入口仍为 `v1.0.11`，需要用户在扩展管理页手动重新加载 `v1.1.1` 解压目录后完成安装态界面确认。

## 20. 合并标题与四排图片密度

- [x] 20.1 更新 proposal、design 和增量规格，明确品牌标题移入商品摘要行右侧、面板收紧和 876px 高视口四排图片可见目标。
- [x] 20.2 增加失败测试，覆盖合并标题 DOM、560–600px 面板、124–134px 媒体区、8px 四列间距、紧凑分组/卡片操作栏和 `v1.1.2`。
  - 修复前聚焦运行 18 项测试，其中 3 项按预期失败：Manifest 和扩展包仍为 `v1.1.1`，面板仍保留独立标题层及旧尺寸契约。
- [x] 20.3 实现合并标题栏和紧凑图片密度，保持拖动、六按钮单行、组内操作、查看器和底部操作不变。
- [x] 20.4 更新中文说明和 `v1.1.2` 包名，运行聚焦、全量、同步、构建、OpenSpec、差异与编码检查。
  - `v1.1.2` 的 42 项商品图聚焦测试、1458 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验、相关差异格式和 20 个本次文本文件的严格 UTF-8/异常替换字符检查均通过；扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.2.zip`。
- [x] 20.5 在 1707×876 等效视口使用真实商品图完成截图对照，确认标题落点、四排图片可辨认、无重叠且底部操作可见；实际 Chrome 安装态仍需手动重载时记录缺口。
  - 应用内浏览器隔离加载当前 `v1.1.2` 脚本后，面板为 `580×876px`，商品摘要与 `GPT-Image2-Studio / 商品图采集` 位于同一排，首张卡片为 `130×160px`、图片媒体区为 `126×126px`、四列间距为 `8px`；图片行顶部为 `191、359、594、762px`，第四排在 `819px` 起始的底部操作栏前清楚露出，且无水平溢出。
  - 15/15 张真实商品截图裁片均加载且无破图；只选主图、反选、全选、单图下载、插件内查看、滚轮缩放和点击空白处关闭均通过，浏览器控制台错误和警告为 0。全视图和标题聚焦合并对照记录在 `artifacts/design-qa/`，`design-qa.md` 最终结果为 passed。未自动修改实际 Chrome 安装态，仍需在扩展管理页手动重载解压目录后确认安装版本。

## 21. GigaB2B 新版商品页识别

- [x] 21.1 在当前真实商品页确认新版容器、图片分类与数量：`#image-show` 为主图，`.options-wrap .options-item` 为带名称的 SKU，页面没有独立详情图区域。
- [x] 21.2 增加真实结构 fixture 和失败回归测试，固定 9 张主图、0 张详情图、6 个独立 SKU，并排除相似商品图片。
- [x] 21.3 更新 GigaB2B 选择器和 SKU 名称提取；同图不同规格分别保留，不猜测生成详情图；同步中文说明、design、增量规格和 `v1.1.3` 版本链路。
- [x] 21.4 运行商品图聚焦测试、public/lib 同步、扩展与 Pages 构建、全量测试、OpenSpec 严格校验、差异和中文编码检查，并生成 `v1.1.3` ZIP。
  - `v1.1.3` 的 43 项商品图聚焦测试、1459 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验、相关差异格式和 15 个本次文本文件的严格 UTF-8 检查均通过；扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.3.zip`。
- [x] 21.5 在当前登录的真实 GigaB2B 商品 `1149324` 上隔离运行工作区采集器，验证实际 DOM 结果。
  - 实测结果为 9 张主图、0 张详情图、6 个 SKU，`skuVariantCount` 为 6；六个 SKU 名称均来自页面规格文本。实际 Chrome 安装实例仍需在扩展管理页手动重载后确认 `v1.1.3` 安装态。

## 22. Temu 新版语义区域与折叠详情采集

- [x] 22.1 在当前登录的真实 Temu 商品 `605854459894642` 上复现 `v1.1.3` 返回 0 张，并确认新版主图库、SKU、详情图、懒加载占位和推荐区控件边界。
- [x] 22.2 增加失败回归测试，覆盖 17 张有序主图、7 张命名 SKU、10 张懒加载详情图、详情区 `See more` 自动展开及推荐区同名控件不被点击。
  - 修复前商品图扩展测试 15 项中 1 项按预期失败，新版 Temu fixture 返回 `no_product_images`；修复后 15 项全部通过。
- [x] 22.3 更新 Temu 稳定语义选择器、详情展开等待和懒加载尺寸判断，保持整页扫描失败关闭与其他平台行为不变。
- [x] 22.4 更新中文说明、Manifest 与扩展包版本链路至 `v1.1.4`。
- [x] 22.5 运行商品图聚焦测试、public/lib 同步、扩展与 Pages 构建、全量测试、OpenSpec 严格校验、差异和中文编码检查，并生成 `v1.1.4` ZIP。
  - `v1.1.4` 的 44 项商品图聚焦测试、1460 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验、相关差异格式和 15 个本次文本文件的严格 UTF-8 检查均通过；扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.4.zip`。
- [x] 22.6 在当前真实 Temu 商品页运行工作区采集器，确认主图、详情图、SKU 图数量和顺序，并验证未点击或采集推荐区。
  - 当前登录的 Temu 商品 `605854459894642` 实测得到 17 张主图、10 张详情图和 7 张 SKU 图，三组 URL 均与对应语义 DOM 的首次出现顺序完全一致；SKU 依次为 Light Blue、Dark Blue、pink color、Purple、Green、Grey、Yellow，推荐区 URL 重叠为 0。当前重载状态已直接挂载 10 张详情图且没有详情区 `See more`，页面外部 `View more / See more items` 保持存在；详情区展开分支由新版 fixture 验证只点击内部控件一次、推荐控件零次。

## 23. 大件云仓原图与当前规格映射

- [x] 23.1 更新 proposal、design 和增量规格，明确大件云仓原图规范化、当前规格与主图库最后一张的映射、主图冻结和卡片轻量预览边界。
- [x] 23.2 增加失败回归测试，覆盖 500×500 主图、74×74 规格缩略图、当前规格缩略图与正确 SKU 图不同、同 URL 跨分类保留及 `x-oss-process` 移除。
- [x] 23.3 实现大件云仓平台 URL 规范化和 SKU 映射，并同步共享 `public/lib`；采集过程不得点击规格或导航。
- [x] 23.4 为大件云仓卡片派生约 300×300 的轻量预览，保持查看、复制和下载使用原图；更新中文说明与 `v1.1.5` 版本链路。
- [x] 23.5 运行商品图聚焦测试、public/lib 同步、扩展与 Pages 构建、全量测试、OpenSpec 严格校验、差异和中文编码检查，并在当前真实商品页复测主图/SKU 数量、原图尺寸和页面状态。
  - `v1.1.5` 的 30 项聚焦测试、39 项商品图相关测试、1461 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验、相关差异格式和 19 个本次文本文件的严格 UTF-8/异常替换字符检查均通过；扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.5.zip`。
  - 在当前登录的真实大件云仓商品 `1149284` 上隔离运行工作区 `v1.1.5` 采集器，结果为 9 张主图、0 张详情图和 6 张 SKU 图，15 个采集项均不含 `x-oss-process`；当前激活规格 `16in+Pink + ABS+Rubber+Steel (Q235)` 对应 `SKU-2`，其 URL 与有序主图库最后一张 `主图-9` 完全一致，同 URL 在主图与 SKU 分类中分别保留。
  - 直接请求采集清单中的真实 CDN URL，抽查主图与非当前规格 SKU 原图均为 `1600×1600`，分别替代页面加载的 `500×500` 与 `74×74` 版本；采集前后页面 URL、9 张主图、6 个规格及当前激活规格均保持不变，采集过程未点击、未 hover、未导航。当前 Chrome 安装实例仍为 `v1.1.3`，受浏览器内部页安全边界限制无法自动重载，需要手动重载 `artifacts/extensions/product-image-collector-unpacked` 后再使用安装态入口。

## 24. 紧凑面板、追加分类选择与分辨率

- [x] 24.1 更新 proposal、design 和增量规格，明确分类追加选择、真实分辨率、紧凑 UI 比例以及按最新截图回调到当前显示环境约 800px 的面板宽度。
- [x] 24.2 增加失败回归测试，覆盖最终 520–540px 面板、约 115–120px 四列媒体区、小按钮、24px 卡片操作栏、分类选择不清空其他选项、完整浅色元数据栏以及大件云仓真实原图元数据。
  - 修复前 16 项扩展测试中 3 项按预期失败：版本仍为 `v1.1.5`，大件云仓仍报告 500×500/74×74 缩略图尺寸，面板仍使用旧尺寸与“只选”覆盖式分类选择。
- [x] 24.3 实现紧凑布局、追加分类选择和分辨率显示；大件云仓元数据失败时不得把 500×500 或 74×74 缩略图尺寸冒充为原图分辨率。
- [x] 24.4 更新中文说明和 `v1.1.7` 版本链路，运行聚焦、全量、同步、构建、OpenSpec、差异和中文编码检查，并生成新版 ZIP。
  - `v1.1.7` 的 16 项扩展测试、46 项商品图相关测试、1462 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验、相关差异格式和 20 个本次文本文件的严格 UTF-8/异常替换字符检查均通过。
  - 扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.7.zip`，ZIP 与解压目录中的 manifest 均为 `1.1.7`。
- [x] 24.5 在当前真实 Temu 商品页隔离加载工作区面板，确认四列、卡片/按钮尺寸、分辨率落点和分类追加选择行为；实际 Chrome 安装态仍需手动重载时记录缺口。
  - 当前 Temu 商品页进入平台 Security Verification，未尝试绕过。改用用户本轮 Temu 页面截图作为页面背景，并在隔离验收页加载已确认可访问的真实 Temu CDN `1254×1254` 商品图；`738×896` 视口下面板为 `520×896px`，四列媒体区为 `114.67×114.67px`，卡片间距 `6px`、操作栏 `24px`、选择按钮 `27px` 高且字号 `11px`，没有水平溢出。
  - 浅色半透明信息条完整显示 `主图-13` 与 `1254×1254`，没有省略、重叠或深黑双标签。反选清空后保留 1 张详情图，再依次追加主图、SKU 图和详情图，选择数按 `0 → 1 → 18 → 25 → 34` 变化，证明分类选择不会清空其他类别；应用内浏览器控制台错误和警告均为 0，`design-qa.md` 最终结果为 passed。
  - 当前 Chrome 安装态仍需手动重载 `artifacts/extensions/product-image-collector-unpacked` 后确认 `v1.1.7`；本轮没有把隔离验收结果冒充为安装态验证。

## 25. 查看器命令与完整标题

- [x] 25.1 更新 proposal、design 和增量规格，明确全屏 cover、原始尺寸、重复命令、透明控制区、精简导航以及标题/统计位置。
- [x] 25.2 增加失败回归测试，覆盖无锁定状态的重复命令、透明按钮、关闭图标、无返回/图片说明、完整换行标题和状态行统计。
  - 修复前 16 项扩展测试首先由 `v1.1.7` 版本断言按预期拦截；更新版本后，透明控制区、无锁定重复命令、精简查看器和完整标题契约全部通过。
- [x] 25.3 实现查看器与标题栏调整，并更新中文说明及 `v1.1.8` 版本链路。
- [x] 25.4 在同一商品图和视口下验证默认完整适配、全屏、原始尺寸、连续重复点击、前后切换、关闭和标题换行；输出截图与 `design-qa.md`。
  - `1707×876` 视口下面板为 `529.27×876px`；长标题完整换为两行，状态行完整显示 34 张选择/总数和 7 个 SKU 规格，标题下方不再存在数量节点。
  - 全屏连续两次均得到 `scale(1.606)`，原始尺寸连续两次均得到 `scale(1)`；两项命令始终可用、无 `aria-pressed` 或锁定类，鼠标触发后活动焦点为空。下一张、边界禁用、透明关闭和控制区边界均通过，控制台错误和警告为 0。
  - 四组最终截图及合并对照保存在 `artifacts/design-qa/product-image-viewer-v1.1.8-*.png`，`design-qa.md` 最终结果为 passed。
- [x] 25.5 运行扩展聚焦、商品图、全量、同步、构建、OpenSpec、差异与中文编码检查，并生成新版 ZIP。
  - `v1.1.8` 的 16 项扩展测试、46 项商品图相关测试、1462 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验、相关差异格式和 16 个本次文本文件的严格 UTF-8/异常替换字符检查均通过。
  - 扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.8.zip`，ZIP 与解压目录中的 manifest 均为 `1.1.8`；ZIP SHA-256 为 `123415530DD42DC80FEE76E3E6809CBB134CD57AF620760CFFE2CD02F42A7248`。

## 26. 标题边界与卡片完整展示

- [x] 26.1 在两个真实 1688 商品页确认商品名位于 `#productTitle .title-content`，经营数据位于同级 `.sell-point-wrapper`，并更新 proposal、design 与增量规格。
- [x] 26.2 增加失败回归测试，覆盖 1688 标题排除经营数据、所有平台单一后缀、白底 contain 完整缩略图、原图操作不变和 SKU 纯规格名称行。
- [x] 26.3 实现标题提取、平台后缀、缩略图与 SKU 规格行，并更新 `v1.1.9` 版本链路及中文说明。
- [x] 26.4 运行扩展与商品图聚焦测试、全量测试、同步、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查，并生成与校验 `v1.1.9` ZIP。
  - `v1.1.9` 的 17 项扩展测试、47 项商品图相关测试、1463 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建、23 项全项目 OpenSpec 严格校验均通过。
  - 扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.9.zip`，ZIP 与解压目录中的 manifest 均为 `1.1.9`；ZIP SHA-256 为 `70191C3B7EBEA77C80BF0CACEF62245CD9AA78B8544FA2E8F879D2AC0B5690E8`。
- [x] 26.5 在真实 1688 商品页或隔离加载当前源码，确认清洁标题、平台后缀、非 1:1 图片完整预览、SKU 规格名称和原图下载行为。
  - 在真实商品 `838592338170` 和 `823645312929` 上隔离运行工作区采集器，两个标题均只含商品名并以单一 `——‘1688’` 结尾；经营数据未进入清单，SKU 名称分别完整保留 8 项和 46 项。
  - `1707×876` 隔离验收页中，四张 `800×1115`、`800×696`、`800×897`、`800×1173` 真实图片全部以白底 `contain` 加载，四个 SKU 中间行只显示规格名，面板无水平溢出。卡片预览、查看器和下载的 URL 分工由回归测试继续确认；实际 Chrome 安装实例仍需手动重载最新解压目录。

## 27. SKU 完整规格名与纯平台标题

- [x] 27.1 更新 proposal、design 和增量规格，明确标题后缀不含引号，以及 SKU 规格名禁止省略并按视觉行统一动态字号。
- [x] 27.2 增加失败回归测试，覆盖旧带引号后缀规范化、无引号纯平台标题、行级字号计算和禁止 `ellipsis`。
- [x] 27.3 实现标题格式与 SKU 行级动态字号，并更新 `v1.1.10` 版本链路及中文说明。
- [x] 27.4 使用真实长规格名完成两行视觉验收，确认每行字号一致、所有文本完整且无卡片溢出。
  - `529.27px` 面板的首行四个长规格统一为 `9.1px`，第二行四个更长规格统一为 `7.8px`；8 个节点均满足 `clientWidth = scrollWidth = 119px`，没有省略号或跨卡片溢出。标题完整显示为无引号的 `商品名——1688`，12 张真实图片全部加载且面板无水平溢出。
- [x] 27.5 重新运行扩展、商品图和全量测试，同步、构建、OpenSpec、差异与编码检查，并生成校验 `v1.1.10` ZIP。
  - 最终版本的 17 项扩展测试、47 项商品图相关测试、1463 项全量测试、88 个 `public/lib` 同步模块、扩展与 Pages 构建以及 23 项全项目 OpenSpec 严格校验均通过。
  - 扩展包生成于 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.10.zip`，ZIP 与解压目录中的 manifest 均为 `1.1.10`；ZIP SHA-256 为 `F00E8804B0447F778C2C0EEA9665EBEDCEECF3B79E0EF249982608B2416D841D`。

## 28. Studio 批次预览 Escape 分层关闭

- [x] 28.1 更新 proposal、design 和增量规格，明确批次列表忽略 `Escape`，放大图中的 `Escape` 只返回列表。
- [x] 28.2 增加失败回归测试，覆盖第一次 `Escape` 关闭放大图、第二次 `Escape` 保持批次弹窗和当前选择。
  - 修复前聚焦测试 7 项中 1 项按预期失败：列表态 `dialog.cancel` 没有调用 `preventDefault()`；嵌入式浏览器会送达 `keydown` 但不生成原生 `cancel`，新增显式键盘路径测试后同样按预期失败。
- [x] 28.3 在批次弹窗自身显式拦截 `Escape`，仅在放大图可见时收起放大图，并保留原生 `cancel` 回退。
- [x] 28.4 运行聚焦测试、全量测试、public/lib 同步检查、OpenSpec 严格校验和浏览器交互验收。
  - 最终 11 项聚焦测试、1464 项全量测试、88 个 `public/lib` 同步模块和 23 项全项目 OpenSpec 严格校验全部通过。
  - 本地 `http://127.0.0.1:3600/#creation` 实测中，放大层按 `Escape` 后只返回列表，列表再次按 `Escape` 仍保持弹窗、1 张选择和当前焦点；点击右上角关闭按钮后弹窗才关闭。

## 28. 长图自然像素查看与选择滚动稳定

- [x] 28.1 更新 proposal、design 和增量规格，明确自然像素基准的 contain、cover、真实 1:1，以及选择变化不得重建列表或改变滚动位置。
- [x] 28.2 增加失败回归测试，覆盖 `800×3700` 长图三种不同缩放、低于 50% 的默认 contain、溢出轴拖动，以及所有选择入口原地同步。
  - `test/product-image-collector-extension.test.mjs` 已覆盖自然像素缩放、动态最小比例、固定舞台坐标层、拖动夹紧和不重建列表的选择同步；当前扩展聚焦套件 17/17 通过。
- [x] 28.3 实现自然像素查看器、动态最小缩放和边界拖动；选择操作改为原地更新卡片、组计数、半选状态与动作可用性，并更新 `v1.1.11` 版本链路。
  - 查看器统一读取 `naturalWidth`/`naturalHeight`，图片在固定坐标层居中并按 contain、cover 或 1:1 更新；单图、组内、全选、反选和分类选择均通过 `commitSelectionChange()` 原地同步。manifest、面板、启动器、导入控制器和扩展包版本链路均为 `1.1.11`。
- [x] 28.4 使用长图完成默认完整显示、全屏 cover、真实 1:1、重复命令、拖动边界与选择滚动位置的视觉和交互验收。
  - `http://127.0.0.1:3611/artifacts/design-qa/product-image-viewer-v1.1.11.html` 实测 `800×3700` 长图默认以 `0.222432` 完整显示；插件内全屏以 `0.61375` 同时覆盖 `490.67×822.67` 舞台两轴；原始尺寸连续执行两次均保持 `scale(1)`。
  - 1:1 状态纵向拖动在约 `+1439/-1438px`、横向拖动在约 `-154px` 夹紧，图像边缘与舞台边缘误差不超过 `0.67px`。真实坐标点击单图、组内全选、反选和全选时，列表 `scrollTop` 始终为 `520`、`scrollHeight` 始终为 `1407`，焦点保留在原控件。
- [x] 28.5 运行扩展、商品图和全量测试，同步、构建、OpenSpec、差异与编码检查，并生成校验 `v1.1.11` ZIP。
  - 扩展聚焦测试 17/17、商品图相关测试 31/31、全量测试 1464/1464 通过；88 个 `public/lib` 模块同步一致，Cloudflare Pages 构建成功，OpenSpec 严格全项目校验 23/23 通过，相关差异无空白错误且未发现 Unicode 替换字符。
  - 已生成 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.11.zip`（36,115 字节），SHA-256 为 `4E66B7E6FB5B282AC04DAE32513C7A3856562074DAF04D62A1918F85892C50D1`；ZIP 内 manifest 版本为 `1.1.11`，包含 9 个预期文件/目录条目。

## 29. 查看器全缩放比例抓取移动

- [x] 29.1 更新 proposal、design 和增量规格，明确默认 contain、继续缩小、全屏和 1:1 均可直接抓取移动，并定义小图与溢出图的轴向边界。
- [x] 29.2 增加失败回归测试，覆盖默认抓取光标、移除溢出门槛、绝对尺寸差边界和 `v1.1.12` 版本链路。
  - 修复前扩展聚焦套件 16/17 通过，版本与新增全比例拖动契约按预期阻止旧实现继续通过。
- [x] 29.3 实现所有缩放比例可拖动，保持图片可见与拖动后不误关闭，并更新 `v1.1.12` 版本链路。
  - 查看图片默认使用抓取光标，不再读取 `data-pannable`；`viewerOffsetBounds()` 对每个轴使用缩放后图片尺寸与舞台尺寸差值绝对值的一半，统一限制小图和溢出图。manifest、面板、启动器、下载文件名及对应测试均更新为 `1.1.12`。
- [x] 29.4 在浏览器实测直接打开、继续缩小、全屏和 1:1 的抓取移动与两类边界。
  - 默认 `800×3700` 长图在 `scale(0.222432)` 下直接显示 `grab`，横向拖动夹紧到 `157px` 且查看器保持打开；`800×696` 常规图缩小至 50% 后拖动夹紧到 `46×238px`，四边仍完全位于舞台内。
  - 同一常规图在全屏 `scale(1.18247)` 下拖动至 `-227px`，在 1:1 下拖动至 `155×64px`，对应边缘误差均不超过 `0.67px`；浏览器控制台 error/warn 为 0。
- [x] 29.5 运行聚焦、全量、同步、构建、OpenSpec、差异、编码和 ZIP 校验。
  - 扩展聚焦测试 17/17、商品图相关测试 31/31、全量测试 1464/1464 通过；88 个 `public/lib` 模块同步一致，Cloudflare Pages 构建成功，OpenSpec 严格全项目校验 23/23 通过，相关差异无空白错误且未发现 Unicode 替换字符。
  - 已生成 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.12.zip`（36,023 字节），SHA-256 为 `5A49F1EEA5378C1BC77EBC575B9B368A59D042238E991304010D9DA86A75629A`；ZIP 内 manifest 版本为 `1.1.12`，包含 9 个预期文件/目录条目。

## 30. 同图同名 SKU 的稳定来源身份

- [x] 30.1 更新 proposal、design 和增量规格，定义可选 `variantKey`、可信平台字段、缺失字段回退和重复 DOM 去重规则。
- [x] 30.2 增加失败回归测试，覆盖大件云仓同图同名不同 `product_id` 全部保留、相同 `variantKey` 去重、共享清单往返和主图/详情图 URL 去重不变。
  - 修复前 33 项聚焦测试中 3 项按预期失败：大件云仓同图同名用例由预期 9 条缩减为 5 条，Amazon 同图同名 ASIN 用例由 2 条缩减为 1 条，共享清单也只保留 1 条。
- [x] 30.3 在采集器和共享清单实现稳定来源变体身份，为已证实字段的适配器生成平台前缀键，并更新 `v1.1.13` 版本链路。
  - 采集器与清单规范化均优先使用 URL 与受限 `variantKey`，没有键时继续使用 URL 与规格名称；GigaB2B 读取 `product_id`，Amazon 读取可证实的 ASIN。manifest、面板、启动器、下载文件名及对应测试均更新为 `1.1.13`，51 项商品图相关测试通过。
- [x] 30.4 使用真实大件云仓页面复核 9 个关联 `product_id` 不再按 5 个图片 URL 缩减，并记录其他平台的实页字段审计边界。
  - `product_id=318720` 真实页以当前源码返回主图 13、详情图 0、SKU 9，来源商品 ID 依次为 `318718`、`318719`、`318720`、`318958`、`318959`、`318960`、`318961`、`657294`、`657297`，均生成 `gigacloud:` 前缀键且显示名称都为 `Silver + Aluminium`。
  - Amazon 当前页可见选项链接提供不同 ASIN，现有已接受 SKU 节点有 ASIN 时使用稳定键；1688 当前 `skuProps`、Temu 的 7 个规格单选项和 SHEIN 的 2 个颜色单选项未暴露可独立验证的来源变体 ID，TikTok Shop 没有新增实页证据，因此这些路径保持 URL 与名称回退且不猜测字段。
- [x] 30.5 运行聚焦、全量、同步、构建、OpenSpec、差异、编码和 ZIP 校验。
  - 扩展聚焦测试 19/19、商品图相关测试 51/51 通过；88 个 `public/lib` 模块同步一致，Cloudflare Pages 与扩展构建成功，OpenSpec 严格全项目校验 24/24 通过，相关差异无空白错误且未发现 Unicode 替换字符。
  - 全量测试 1470 项中 1465 项通过；5 项失败均位于本次未修改的 Listing 尺寸字段和营销文案校验，已保留为明确的仓库现有缺口，没有扩大本次修复范围。
  - 已生成 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.13.zip`（36,572 字节），SHA-256 为 `4107152129708677E07A46CBE842DAD688722AEE5FAD09D6174377CAD0375534`；ZIP 与解压目录 manifest 均为 `1.1.13`，包含 9 个预期文件/目录条目。

## 31. SHEIN 图片边界与主图分辨率

- [x] 31.1 在真实 SHEIN 商品页确认当前主图库、详情图、SKU 图与误采集媒体的 DOM/结构化数据边界，并同步 proposal、design 与增量规格。
  - 商品 `426980845` 的 JSON-LD `ProductGroup` 声明 8 张 `_thumbnail_900x.webp` 主图，当前大图实际加载为 `900×1199`；同一画廊的 8 张 DOM 缩略图为 `220×293`。页面 `cccDetailsTemplate.detailImg` 为 `0`，因此该页没有真实详情长图；旧版宽泛 `[class*='goods-detail'] img` 误收了画廊缩略图、大图、社交图标、颜色 SKU 和店铺图片。
  - 当前站点样式确认真实详情媒体使用 `.details-pic__img img`；颜色 SKU 仍位于已知颜色选择器。成功标准为该页返回 8 张主图、0 张详情图和 2 张 SKU 图，并排除属性图标、店铺、评价、推荐及其他页面媒体。
  - 页面状态 `allColorDetailImages` 为黑色 SKU 缩略图哈希 `177822742903586188477cd9c5043cd017da11d252` 明确提供 `_thumbnail_900x.webp`，白色 SKU 也与当前高分辨率首图哈希一致；两张 SKU 可基于声明证据升级而无需猜测 URL。
- [x] 31.2 增加失败回归测试，覆盖 `ProductGroup` 主图、当前 `main-picture` 回退、明确详情区域、误采集媒体排除、Open Graph 跨格式去重和声明高清 SKU URL。
  - 修复前 21 项扩展测试中 3 项按预期失败：SHEIN `ProductGroup` 返回 0 张主图，当前 `main-picture` 回退返回未找到，宽泛详情选择器把画廊缩略图、社交图标、属性图、店铺图和 SKU 图共 5 类错误媒体加入详情图。
- [x] 31.3 实现 SHEIN 适配器边界修复并更新 `v1.1.14` 版本链路。
  - 适配器识别 JSON-LD `ProductGroup`、当前 `main-picture` 和 `.details-pic__img`，移除整页 `goods-detail` 详情选择器；同一 SHEIN 资源跨尺寸/格式去重，并只按 `allColorDetailImages.origin_image` 的相同路径哈希把颜色缩略图升级为声明高清 URL。manifest、面板、启动器、下载文件名与端点测试版本均为 `1.1.14`。
- [x] 31.4 运行扩展聚焦、商品图相关、全量、同步、构建、OpenSpec、差异、编码和 ZIP 校验。
  - 扩展聚焦测试 21/21、商品图相关测试 53/53、全量测试 1472/1472 通过；88 个 `public/lib` 模块同步完成，扩展与 Cloudflare Pages 构建成功，OpenSpec 严格全项目校验 23/23 通过，相关差异无空白错误，15 个本次文件通过严格 UTF-8 与替换字符检查。
  - 已生成 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.14.zip`（37,081 字节），SHA-256 为 `3853C4A51B6F8C63C63159D82D9B196E4CDCAB8F47B55BFB861C5283E5B3623D`；ZIP 与解压目录 manifest 均为 `1.1.14`，包含 9 个预期文件/目录条目。
- [x] 31.5 在真实 SHEIN 页面隔离运行当前源码，复核分类数量、最终 URL/分辨率和页面状态，并清理代理浏览器标签页。
  - 商品 `426980845` 使用当前源码返回主图 8、详情图 0、SKU 2；8 张主图及 White/Black 两张 SKU 都使用页面明确声明的 `_thumbnail_900x.webp` URL，逐项直接打开后自然尺寸均为 `900×1199`。Twitter、Pinterest、Facebook、属性图、店铺图、评价图和推荐图均未进入结果。
  - 采集前后商品页 URL 与已选颜色状态一致，未点击规格或改变页面；本次代理浏览器创建的商品与图片检查标签页已全部清理。

## 32. TikTok Shop 当前路由、声明图片与分辨率

- [x] 32.1 在两个真实 TikTok Shop 商品页确认当前路由、声明数据、主图/详情图/SKU 图、高清尺寸和无关页面媒体边界，并同步 proposal、design 与增量规格。
  - 商品 `1732462294641644144` 的匹配 `product_model` 声明 9 张主图、8 张详情图和 0 张 SKU 图；主图最高为 `2000×1500`，详情图包含 `1065×3937` 长图。页面共 126 张图片并含推荐、Logo、头像等无关媒体，因此不得使用整页图片兜底。
  - 商品 `1729449869154554187` 的 `Lace` 销售属性声明 3 个带图 `property_value_id`，尺寸为 `1200×1200`、`1080×1080`、`1200×1200`；无图 `Size` 属性及其 SKU 组合复用这些图片，不应重复生成。
- [x] 32.2 增加失败回归测试，覆盖新旧路由、匹配商品模型、9/8/0 分类、3 个稳定 SKU、声明分辨率、同资源身份、无关图片排除和 `v1.1.15` 权限/版本链路。
  - 修复前 44 项商品图聚焦测试中 8 项按预期失败：当前 `shop.tiktok.com` 来源被判定为不支持，三个当前路由采集用例无法返回结果，Manifest 仍为 `v1.1.14`，共享清单与代理也拒绝当前来源。
- [x] 32.3 实现 TikTok Shop 当前路由、声明数据读取、稳定 SKU 身份、保守 DOM 回退与共享清单同资源去重，并更新 `v1.1.15` 版本链路。
  - 当前适配器支持新旧域名，受限解析匹配商品 ID 的 `__MODERN_ROUTER_DATA__`，以声明宽高和 `url_list` 生成主图/详情图，并按 `property_value_id` 生成稳定 SKU 键。声明完整时不追加 DOM；回退要求主图和详情图的 `alt`、`title` 与 `og:title` 一致，并优先保留 `src` 中的高清地址。Manifest、面板、启动器、导入控制器和扩展包版本链路均为 `1.1.15`。
- [x] 32.4 运行扩展聚焦、商品图相关、全量、同步、构建、OpenSpec、差异、编码和 ZIP 校验。
  - 首轮 44 项 TK/清单/代理聚焦测试与 57 项商品图相关测试全部通过；全量测试 `1478/1478` 通过。88 个 `public/lib` 模块同步完成，扩展与 Cloudflare Pages 构建成功，OpenSpec 严格全项目校验 `24/24` 通过，相关差异无空白错误且未发现 Unicode 替换字符。
  - 已生成 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.15.zip`，SHA-256 为 `C6872588ADD6AC8A3F6DDB940C5EA16317B926FFA99609CA7D80E96F3D9FE1D3`；ZIP 内 Manifest 为 `1.1.15`，包含采集器与共享平台协议。
- [x] 32.5 在两个真实 TikTok Shop 页面隔离运行当前源码，复核分类、URL、分辨率、无关媒体与页面状态，并清理代理浏览器标签页。
  - 商品 `1732462294641644144` 使用当前源码返回主图 9、详情图 8、SKU 0；所有 URL 都是匹配商品模型声明的 `ttcdn-us.com` 地址，宽高顺序与声明一致，最高主图 `2000×1500`，长详情图 `1065×3937`，未追加页面推荐、Logo、头像、评价或缩放 DOM 副本。
  - 商品 `1729449869154554187` 返回主图 9、详情图 9、SKU 3，三个 SKU 键分别为 `tiktok:7664781240428398350`、`tiktok:7664781240428414734`、`tiktok:7664781240428431118`，没有按 Size 组合重复。直接加载代表性 URL 后自然像素为 `2000×1500`、`1065×3937`、`1200×1200`、`1080×1080`、`1200×1200`，与清单声明完全一致。
  - 两次采集前后的商品页 URL、滚动位置和可见选择状态均保持不变；本轮代理浏览器创建的商品与分辨率检查标签页已全部清理。安装态仍需用户在扩展管理页重新加载 `artifacts/extensions/product-image-collector-unpacked` 才会切换到 `v1.1.15`。

## 33. 查看器初始视图复位

- [x] 33.1 根据用户三张截图确认末位 `1:1` 命令显示自然像素长图而非初始完整适配，且快速双击当前会关闭查看器；同步 proposal、design 与增量规格为新的复位行为。
- [x] 33.2 增加失败回归测试，覆盖复位时结束拖动、旋转归零、位移归零、重新计算 contain 比例、按钮与双击共用复位，以及双击不关闭查看器。
  - 修复前扩展聚焦测试 24 项中仅查看器契约 1 项按预期失败，旧“原始尺寸”提示和 `1:1` 行为被准确命中。
- [x] 33.3 实现“恢复初始视图”图标命令，让图片加载、工具栏点击和快速双击共用同一复位函数，并更新 `v1.1.16` 版本链路。
- [x] 33.4 运行商品图聚焦、全量、同步、扩展与 Pages 构建、OpenSpec、差异、编码和 ZIP 校验。
  - 88 个 `public/lib` 模块同步完成；商品图相关测试 `57/57`、全量测试 `1479/1479`、OpenSpec 严格全项目校验 `23/23` 通过，扩展与 Cloudflare Pages 构建成功。
  - 已生成 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.16.zip`（38,846 字节），SHA-256 为 `65181B7364F1E10483B1866713D8E8E918BC43FEFE0FCAC2F580C720AFF4A537`；ZIP 与解压目录 Manifest 均为 `1.1.16`，包含 9 个预期条目。
- [x] 33.5 在隔离验收页实测放大、旋转、拖动后的按钮复位与快速双击复位，并确认查看器保持打开。
  - `800×3700` 长图首次打开为 `translate3d(0px, 0px, 0px) rotate(0deg) scale(0.222432)`；旋转、放大并拖动后为 `translate3d(60px, 90px, 0px) rotate(90deg) scale(0.175499)`，点击“恢复初始视图”准确返回首次打开状态且查看器未关闭。
  - 再次改变为 `rotate(270deg) scale(0.152608)` 后快速双击图片，同样恢复到零位移、零旋转和 `scale(0.222432)`；查看器保持打开，验收页控制台 `warn/error` 为 0。

## 34. Amazon 当前 ASIN 图库与声明 SKU

- [x] 34.1 在真实 Amazon 商品 `B0F1MCXQ3M` 确认当前地址 ASIN、ATF 五张主图、BTF 四个带图规格、A+ 四张详情图，以及 40×40 缩略图、其他规格隐藏大图、评价图和占位图边界。
- [x] 34.2 增加真实结构 fixture 和失败回归测试，固定 5 张主图、4 张详情图、4 个稳定 ASIN SKU、声明顺序、规范化原图 URL、未知初始分辨率及无关媒体排除。
- [x] 34.3 实现受限 ATF/BTF 解析、当前 ASIN 校验、安全 JavaScript 字符串解码和声明优先回退，并更新 `v1.1.17` 版本链路。
- [x] 34.4 运行商品图聚焦、全量、public/lib 同步、扩展与 Pages 构建、OpenSpec 严格校验、差异、编码和 ZIP 校验。
  - 商品图相关测试 `59/59`、全量测试 `1481/1481`、OpenSpec 严格校验 `23/23` 通过；扩展与 Pages 构建成功，本次修改范围 `git diff --check` 和中文替换字符检查通过。仓库级差异检查仅报告未由本次修改触碰的 `openspec/specs/creation-mode/spec.md` 末尾空行。
  - 已生成 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.17.zip`（40,671 字节），SHA-256 为 `69A2FD715CBBAC92D1396695181DFB11EB2AE34B37B934F7C9DE8E204E6F8785`；ZIP Manifest 为 `1.1.17`，8 个文件与工作区对应源码完全一致。
- [x] 34.5 在当前真实 Amazon 商品页隔离运行工作区采集器，复核分类、URL、规格、分辨率和无关媒体，并保持页面地址、滚动与规格状态不变。
  - 商品 `B0F1MCXQ3M` 的工作区采集器实测为 5 张主图、4 张 A+ 详情图、4 个带稳定 `amazon:<ASIN>` 键的 SKU；结果未包含其他 ASIN 声明、40×40 缩略图、隐藏规格大图、评价图或占位图。
  - 规范化原图加载后，5 张主图均为 `2000×2000`；4 张详情图均为 `500×500`；4 张 SKU 中便携装为 `1448×1671`，其余 3 张为 `2000×2000`。采集前后页面 URL、横纵滚动位置和已选规格状态一致。

## 35. Studio 菜单完整插件说明

- [x] 35.1 更新 proposal、design 和增量规格，定义商品图采集插件名称在鼠标悬停和键盘聚焦时的完整说明、响应式位置、可访问关联与点击行为边界。
- [x] 35.2 增加失败布局回归，覆盖真实 tooltip 节点、支持平台与能力文案、安全边界、hover/focus 显示状态和单列布局位置。
- [x] 35.3 实现创作工具菜单说明提示并更新根 README，保持点击只下载 ZIP 且浏览器安装或更新仍由用户确认。
- [x] 35.4 运行商品图相关测试、全量测试、Pages 构建、OpenSpec 严格校验、差异与编码检查，并在真实工作台验收说明完整可见且不被菜单裁切。
  - 商品图相关测试 `59/59`、布局与关联缓存测试 `176/176`、全量测试 `1481/1481`、OpenSpec 严格校验 `23/23` 通过；public/lib 同步检查、Pages 构建、本次范围差异和中文编码检查通过。
  - 在 `1280×720` 真实工作台打开创作菜单，hover 与键盘聚焦均使完整说明以 `opacity: 1`、`visibility: visible` 显示；说明框宽 340px，位于视口内且未被菜单裁切或遮挡名称。

## 36. 采集面板标题与图片信息覆层优化

- [x] 36.1 根据用户截图同步 proposal、design 和增量规格，定义左侧纯商品标题、右侧动态平台名称及相对居中布局，并降低图片名与分辨率覆层遮挡。
- [x] 36.2 增加失败回归测试，覆盖六个平台名称映射、展示层平台后缀移除、标题区居中、元数据透明度与 `v1.1.18` 版本链路。
  - 修复前 26 项扩展聚焦测试中仅界面与版本契约用例按预期失败，Manifest 仍为 `1.1.17`；其余 25 项平台采集与安全边界测试通过。
- [x] 36.3 实现头部展示辅助函数、平台名称节点和更透明的图片元数据覆层，并更新扩展版本链路。
  - 面板按 `manifest.source.platform` 映射六个平台名称，展示层只移除与该名称匹配的末尾标准后缀；清单标题及下载数据不变。右侧标题区使用网格居中，图片元数据背景调整为 `rgba(248, 250, 252, 0.64)` 并把模糊收紧到 2px。
  - 35 项扩展、打包与 Studio 导入聚焦测试全部通过，88 个 `public/lib` 模块同步检查通过。
- [x] 36.4 运行商品图聚焦测试、全量测试、public/lib 同步、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查。
  - 当前工作区扩展聚焦测试 `28/28` 和 `v1.1.18` 扩展构建通过；在 `HEAD + 本次插件改动` 的隔离快照中，全量测试 `1416/1416`、82 个 `public/lib` 模块同步检查、扩展构建、Pages 构建及 OpenSpec 严格校验 `20/20` 通过。
  - 当前工作区整体同步检查仅被并行修改的 `creation-listing-view.mjs` 差异阻断；该文件不属于本次范围，未改动或回退。
- [x] 36.5 在与参考截图等效的真实页面状态复核标题分工、平台名称居中、信息条透图程度、文字可读性和无重叠，并更新 `design-qa.md`。
  - Chrome 在 `1707×932` 与 `480×844` 视口加载当前 `v1.1.18` 脚本和 15 张真实测试图片；左侧标题不含 `——1688`，右侧显示 `1688`，宽窄状态均无标题/平台/操作按钮重叠及水平溢出。
  - 信息条计算样式为 `rgba(248, 250, 252, 0.64)` 与 `blur(2px)`；目标截图、完整实现、头部和信息条局部已同框比较，未发现可执行的 P0、P1 或 P2 问题。页面与采集器控制台无错误；浏览器记录到的两条错误来自无关的已安装扩展 `hdgbmcpjcflbhcgdgbdaooeohnjfabhi`。

## 37. Studio 批次选择滚动与大屏尺寸

- [x] 37.1 根据用户截图补充 Studio 批次弹窗的大屏宽高上限、安全视口边距、内部滚动和自适应卡片列约束，并在增量规格与设计中记录。
- [x] 37.2 增加回归测试：打开候选批次并滚动列表后，点击全选、反选及三个分类选择按钮不会替换候选节点或改变列表滚动位置。
- [x] 37.3 将选择与忙碌状态更新改为现有卡片、复选框和操作按钮的原地同步，保持焦点、懒加载缩略图和弹窗内部滚动状态。
- [x] 37.4 运行商品图聚焦测试、全量测试、`public/lib` 同步、OpenSpec 严格校验和差异/编码检查。
  - 商品图控制器与布局聚焦测试 `9/9`、全量测试 `1503/1503`、`public/lib` 同步检查 `88/88`、全项目 OpenSpec 严格校验 `23/23` 通过；变更范围 `git diff --check` 与中文替换字符检查通过；Pages 与扩展构建均成功。
- [x] 37.5 在接近 `2559×1314` 的桌面视口及窄视口打开真实 Studio，确认弹窗尺寸接近红框、候选列表可滚动且顶部批量选择不会上移滑块。
  - `2559×1314` 实测弹窗为 `1980×1180`；默认 `1280×720` 为 `1248×688`；`420×760` 为 `402×742`。默认和窄视口滚动候选列表后点击全选、反选、选择主图、选择详情图、选择 SKU，`scrollTop` 均保持不变；候选卡片数量保持 30。

## 38. 批量复制图片到聊天软件

- [x] 38.1 根据用户需求同步 proposal、design 和增量规格，定义独立“复制图片”按钮、Windows FileDrop 语义、本地 Studio 页面桥接、部分失败反馈和安全边界。
- [x] 38.2 增加失败回归测试，覆盖三按钮布局、当前选中项、本地页发现、临时图片准备、FileDrop 写入、部分失败、Cloud 不支持及 `v1.1.19` 版本链路。
  - 实现前本地剪贴板测试因模块不存在而失败，扩展聚焦测试准确拦截未知消息与旧 `v1.1.18` 版本；新增 API 契约也准确拦截旧路由表。
- [x] 38.3 实现受限本地剪贴板模块、Local API、扩展消息与按钮，并更新中文说明及版本链路。
  - 当前本地剪贴板测试 `3/3`、扩展测试 `27/27`、API 契约 `6/6`、商品图端点 `3/3`、扩展打包测试 `2/2` 通过；`public/lib` 已同步 88 个模块。
- [x] 38.4 增加复制成功提示的失败回归测试并实现插件内半透明淡入淡出反馈、连续触发重置及 `v1.1.20` 版本链路。
  - 实现前扩展与打包聚焦测试准确拦截旧 `v1.1.19` 版本；实现后扩展、剪贴板、API 契约、商品图端点和扩展打包聚焦测试 `41/41` 通过，面板与本地剪贴板模块语法检查通过。
- [x] 38.5 运行商品图聚焦测试、全量测试、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查。
  - 最终全量测试 `1503/1503`、`public/lib` 同步检查 `88/88`、OpenSpec 严格校验 `23/23` 通过；扩展与 Pages 构建成功，`git diff --check` 无错误，25 个本次相关中文文件的严格 UTF-8 与替换字符检查通过。
- [x] 38.6 在真实 Chrome 插件和 Windows 剪贴板中验证多张选中图片形成 FileDrop 文件列表，并复核三按钮、成功提示动画及窄视口无溢出或重叠。
  - 最终 `v1.1.22` 安装态在真实 1688 商品页选择 31 张图片后显示“已复制 31 张图片，可直接粘贴到聊天软件。”；三按钮顺序保持为“复制到 Studio”“复制图片”“下载所选”。
  - 成功提示在约 750ms 采样为可见，约 2.25s 采样为隐藏；Windows `Get-Clipboard -Format FileDropList` 返回 `Count=31`、`UniqueCount=31`、`ExistingCount=31`。构建重载造成的旧页面上下文失效通过刷新商品页恢复，未再出现旧错误。

## 40. 独立剪贴板助手与几百张复制

- [x] 40.1 根据用户反馈和 HTTP 404 复现结果，把“复制图片”从 Studio 页面桥接改为扩展专用 Windows Native Messaging host；同步 proposal、design 与增量规格，定义稳定扩展来源、1000 张清单、4 MiB 请求和 512 MiB 图片总量边界。
- [x] 40.2 增加失败回归测试，覆盖不查询本地 Studio 标签页、固定 host 消息、助手安装文件、异步准备后的专用 STA 调度，以及 500 张图片完整进入一次 FileDrop 列表且清单不被 120 张上限拒绝。
  - Native host、扩展、打包和 API 聚焦测试 `35/35` 通过；扩展测试中的 `tabs.query()` 保护断言确认直接复制不发现或依赖 Studio 标签页；`--self-test-clipboard-apartment` 返回 `STA`。
  - 当前安装助手执行 `--self-test-filedrop 500` 返回 `{"ok":true,"count":500}`，系统剪贴板随后读取为 `Count=500`、`UniqueCount=500`、`ExistingCount=500`。
- [x] 40.3 实现受限 Windows 助手、当前用户 Chrome/Edge 安装与卸载脚本、扩展 Native Messaging 调用和明确的未安装错误，删除 Studio 剪贴板端点依赖，并将最终 FileDrop 写入固定到专用 STA 线程。
  - 助手只接受固定 `copy-images` 消息，重新校验来源、CDN、重定向、MIME、单图/总量和文件名；安装脚本注册当前用户 Chrome/Edge host，卸载脚本删除对应注册项与目录；最终 `Clipboard.SetFileDropList` 在专用 STA 线程执行并仅对 `ExternalException` 做有界重试。
- [x] 40.4 更新 `v1.1.22` 扩展版本链路并重建包含可审查助手源码、已编译程序和安装脚本的交付包。
  - 交付包为 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.22.zip`（66,837 字节，SHA-256 `0D49791A5DB169D596A8439D12CD38B6895E9DF32532A97C4A58EBF3E534B650`）；解压目录包含助手源码、EXE、安装/卸载脚本，安装后的 EXE 与解压目录 SHA-256 一致。
- [x] 40.5 运行聚焦与全量测试、public/lib 同步、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查。
  - 全量测试 `1505/1505`、`public/lib` 同步 `88/88`、Pages 构建、扩展构建和 OpenSpec 严格校验 `23/23` 通过；聚焦范围 `35/35` 通过。
- [ ] 40.6 在 Windows 系统剪贴板实测 500 个独立文件路径，并在 Studio 关闭状态下验证当前 Chrome 商品页的安装态成功提示与所选数量。
  - 已完成 Windows `500/500` FileDrop 自检及最终 Chrome 安装态 `31/31` 验收；当前证据还包括扩展明确不查询 Studio 标签页的回归断言，但未为此额外关闭用户现有的 Studio 标签页/服务器。

## 39. Studio 批次图片尺寸与插件一致的放大查看

- [x] 39.1 将 Studio 批次网格的桌面最小卡片宽度调整到约 210px，使最大弹窗内容区自然最多显示 8 列，同时保留窄视口列数回退。
  - `2559×1314` 大屏实测弹窗约为 `1980×1180px`，内容区自然排出 8 列、单卡约 `231px`；`390×844` 窄视口弹窗约为 `372×826px`，网格回退为 2 列且页面无横向溢出。
- [x] 39.2 将 Studio 批次查看器改为与插件一致的舞台内覆盖层：同源代理图片、contain 初始视图、cover 全屏、左右旋转、1.15 倍滚轮/按钮缩放、400% 上限、边界拖动、双击复位、前后导航、外部点击关闭和分层 Escape。
  - 通过同源代理加载真实 AliCDN `600×800` 图片后，初始 contain 为 100%，cover 为 325%，按钮与滚轮缩放均按 1.15 倍变化并在 400% 截止；374% 下横向拖动准确夹紧到 `146px` 边界，缩小时位移随新边界回收。
  - 左右旋转、按钮和双击复位、前后导航切图复位、图片外点击关闭均通过；第一次和第二次 `Escape` 都只保持批次弹窗，查看器可见时第一次仅收起查看器。
- [x] 39.3 增加控制器、布局和 OpenSpec 回归覆盖，确认查看器操作不改变批次选择、列表节点或滚动位置。
  - 控制器与布局聚焦测试 `10/10`、商品图相关聚焦测试 `63/63` 通过，覆盖同源代理、插件同款控件、分层 Escape、选择原地同步和滚动位置保持。
- [x] 39.4 同步共享控制器到 `public/lib/` 并运行商品图聚焦测试、全量测试、public/lib 同步、构建与 OpenSpec 严格校验。
  - `public/lib` 同步检查 `88/88`、商品图聚焦测试 `63/63`、全量测试 `1505/1505`、Pages 构建、扩展构建及 OpenSpec 严格校验 `23/23` 均通过。
- [x] 39.5 在大屏与窄视口真实 Studio 页面中复核一行 8 张、查看器工具栏、缩放/拖拽/旋转/导航和 Escape 分层行为。
  - 大屏实测 6 个工具按钮和 2 个导航按钮均可用；窄视口查看器约为 `371×825px`，工具栏约 `226×41px`，contain 59%、cover 94%、旋转后 contain 44%，缩放拖动、切图复位和分层 Escape 均通过且控件无重叠。刷新最新资源后重新加载真实代理图片，9 个 SVG 图标全部渲染且新增 `warn/error` 为 0。

## 41. Studio 批次卡片完整预览与双区操作栏

- [x] 41.1 根据用户截图同步 proposal、design 和 Creation 增量规格，定义固定 1:1 媒体区、纯白补边、完整 `contain` 预览，以及左信息/右放大两个独立底部区域。
- [x] 41.2 增加回归断言，覆盖 `object-fit: contain`、纯白媒体背景、错误状态、信息块与放大按钮的 DOM 顺序和稳定宽度。
- [x] 41.3 实现共享控制器与 `public/lib` 镜像：信息节点移出图片层，媒体层保留加载失败提示，初版操作栏使用左侧可收缩信息块和右侧固定 42px 放大块。
- [x] 41.4 在真实 Studio 视图复核竖图完整显示、白色留白、两个底部区域、桌面 8 列和窄屏无溢出；聚焦控制器/布局测试通过。
  - `2559×1314` 实测弹窗 `1980×1180`、网格计算为 8 列、卡片宽约 `231px`；`390×844` 实测弹窗 `372×826`、网格回退 2 列，媒体与操作栏无水平溢出。Amazon 真实 `1448×1671` 图片在白色 `contain` 媒体区完整显示，操作栏左信息块与右 `42px` 放大块均稳定。
- [x] 41.5 运行全量测试、`public/lib` 同步、Pages 与扩展构建、OpenSpec 严格校验、差异和中文编码检查，并完成同框设计 QA。
  - 全量测试 `1505/1505`、`public/lib` 同步检查 `88/88`、OpenSpec 严格校验 `23/23` 通过；Pages 与扩展构建成功。
  - 用户卡片局部与当前实现已在 `artifacts/design-qa/studio-import-card-contain-focused-comparison.png` 同框检查；成功图片的可访问名称不再包含隐藏的错误文案，页面控制台 `warn/error` 为 0，`design-qa.md` 最终结果为 `passed`。
- [x] 41.6 根据追加截图增加“全不选”、橙色高对比选中态与底部左右等宽回归断言，覆盖空选择结果、卡片原地同步和确认按钮禁用。
- [x] 41.7 将操作栏改为两个 `1fr` 区域，选中卡片与媒体改为橙色外框/内描边，并把“全不选”接入现有批量选择状态同步与 `public/lib` 镜像。
- [x] 41.8 在真实 Studio QA 批次复核橙色选中边界、全不选交互、左右等宽、放大查看和窄屏布局，并运行全量测试、构建、同步及 OpenSpec 严格校验。
  - `733×896` 视口中 6 张已选卡片的卡片与媒体边框均计算为 `rgb(255, 138, 31)`，信息区和放大区各为 `157px`；点击“全不选”后卡片与复选框选择数都变为 0、候选仍为 6 张且确认按钮禁用。重新全选后放大查看正常打开并保持 6 张选择。
  - `390×844` 视口中弹窗为 `372×826px`、网格为 2 列，卡片信息区与放大区各为 `78.67px`；6 个选择命令保持同一行并由计数工具栏横向滚动访问，候选区 `clientWidth` 与 `scrollWidth` 均为 `355px`。
  - 聚焦控制器/布局测试 `10/10`、全量测试 `1506/1506`、`public/lib` 同步检查 `88/88`、OpenSpec 严格校验 `23/23` 通过；Pages 与扩展构建成功。

## 42. 悬浮入口自恢复与大批量响应优化

- [x] 42.1 根据用户反馈同步 proposal、design 与商品图采集增量规格，定义站内 SPA 路由入口、失效实例替换、消息超时释放和长批量操作期间的非阻塞交互。
- [x] 42.2 增加失败回归测试，覆盖站点级轻量入口匹配、完整 URL 二次校验、入口超时恢复、面板分操作超时、失效实例替换、大批 SKU 单次测量和 `v1.1.23` 版本链路。
  - 实现前聚焦范围 `31/32` 通过，唯一失败准确停在清单仍为 `v1.1.22`；后续同一用例已包含站点级匹配、控制器、超时和布局断言。OpenSpec 变更严格校验通过。
- [x] 42.3 实现入口与面板控制器清理、URL 变化同步、有界消息等待、传输期间可用的选择/查看/关闭操作，以及低成本卡片渲染。
  - 聚焦范围 `33/33` 通过；执行级入口回归覆盖 SPA 进入/离开商品路由、旧 DOM 清理、同版本入口控制器原地复用、无响应打开请求的超时释放与可见错误。面板使用分操作超时，并把大批 SKU 规格布局从反复校正改为批量测量后一次应用。
- [ ] 42.4 重建并重新加载安装态扩展，在无需点击扩展栏 action 的真实商品页验证首次打开、连续开关、重新采集和直接复制。
  - 已重建最终 `v1.1.23` ZIP、解压目录和 Native host。真实 1688 页面未点击扩展栏 action，网页入口连续开关 3 次始终只有 1 个 `v1.1.23` 面板和 31 张图片；重新采集 `31/31`，直接复制完成后 Windows FileDrop 为 `31/31/31`。
  - Chrome 当前入口控制器仍为缓存的 `v1.1.22`；站点级 `v1.1.23` Manifest 需要用户在扩展管理页手动点击一次“重新加载”，因此本项保持未完成，不把动态面板验收记作新清单安装态验收。
- [x] 42.5 运行商品图聚焦测试、全量测试、`public/lib` 同步、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查。
  - 扩展、打包和 Native host 聚焦测试 `33/33`，商品图相关测试 `67/67`，全量测试 `1506/1506`，`public/lib` 同步检查 `88/88` 通过；扩展和 Pages 构建成功。
  - OpenSpec 严格全项目校验 `23/23` 通过；仓库级与本次范围 `git diff --check` 无空白错误，14 个相关中文文件通过严格 UTF-8 与替换字符检查。
  - 最终 ZIP 为 68,201 字节，SHA-256 `9D29E79ACD5E8A1A6E09A75BB5AE805B1196F822A6F760B1391DFE5CBF460B76`；最终安装助手线程自检为 `STA`，安装与解压目录 EXE 哈希一致。

## 43. 黑色图片信息条与橙色选中边框

- [x] 43.1 根据用户截图同步 proposal、design 与商品图采集增量规格，定义黑色半透明信息条、白色文字和橙色选中边框，并保持卡片布局与行为不变。
- [x] 43.2 增加失败回归断言，覆盖 `rgba(0, 0, 0, 0.62)` 信息条、白色文字和 `#e9542a` 选中边框。
  - 实现前聚焦用例按预期失败，唯一首个失败点为卡片仍使用旧蓝色选中边框。
- [x] 43.3 实现采集窗视觉调整，并把 Manifest、入口、面板、Native host、ZIP 名称和相关测试统一更新到 `v1.1.24`。
- [x] 43.4 运行聚焦测试、全量测试、`public/lib` 同步检查、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查。
  - 商品图相关聚焦测试 `67/67`、全量测试 `1506/1506`、`public/lib` 同步检查 `88/88` 和 OpenSpec 严格全项目校验 `23/23` 通过；扩展与 Pages 构建成功。
- [x] 43.5 在真实商品页关闭并重新打开面板，复核黑色信息条、白字可读性、橙色选中边框和布局稳定性。
  - 真实 Chrome 1688 商品 `1061922615487` 打开的动态面板为 `v1.1.24`，31 张已选卡片的计算样式为 `rgba(0, 0, 0, 0.62)` 黑色信息条、白字、`blur(2px)` 和 2px `rgb(233, 84, 42)` 橙色边框；信息文字全部位于媒体区内且无溢出，方形媒体区约 `116.98px`、操作栏 24px、面板约 `529.27px`，与修改前布局一致。
  - 当前采集扩展 ID 没有匹配到新的 `warn/error`；最终 ZIP 为 68,211 字节，SHA-256 `D04E570928E4E8C342FBD23CD0BEAD43D705471D4C831B9EEBAFE62BC3F23FA4`。Chrome 清单仍需在扩展管理页手动重新加载后才会把缓存入口从 `v1.1.23` 更新到 `v1.1.24`，不影响本轮已实测的动态面板样式。

## 44. 青绿色勾选状态与 SKU 名称强调

- [x] 44.1 根据用户截图同步 proposal、design 与商品图采集增量规格，把扩展选中边框、卡片勾选框和组内全选框统一为青绿色，并提高 SKU 名称行辨识度；黑色半透明图片信息条和卡片布局保持不变。
- [x] 44.2 增加失败回归断言，覆盖 `#0f766e` 选中边框与两类复选框，以及 SKU 名称行的 `#e7f5f2` 背景、`#0b5f57` 文字和 `#9bcfc4` 上边框。
  - 实现前聚焦用例按预期失败，首个失败点为选中卡片仍使用旧橙色边框。
- [x] 44.3 实现采集窗视觉调整，并把 Manifest、入口、面板、Native host、ZIP 名称和相关测试统一更新到 `v1.1.25`。
- [x] 44.4 运行商品图聚焦测试、全量测试、`public/lib` 同步检查、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查，并记录最终 ZIP。
  - 本项随 `v1.1.26` 最终版本完成：商品图聚焦测试 `67/67`、全量测试 `1508/1508`、`public/lib` 同步检查 `88/88` 和 OpenSpec 严格全项目校验 `23/23` 通过；扩展与 Pages 构建成功。最终 ZIP 为 70,432 字节，SHA-256 `7B07EFA0B3F87201EEA66F235B341EE0625B56A16598CCB35A26233963E728CA`。
- [x] 44.5 在真实商品页复核青绿色勾选和边框、SKU 名称行计算样式、固定卡片尺寸与文本无溢出，并保存设计 QA 截图。
  - 真实 Chrome 1688 商品 `1061922615487` 动态加载当前工作区面板后展示 31 张已选图片和 19 个 SKU 规格；白天选中边框与勾选为 `rgb(15, 118, 110)`，SKU 名称行为背景 `rgb(231, 245, 242)`、文字 `rgb(11, 95, 87)`、上边框 `rgb(155, 207, 196)`。媒体区约 `114.67px`、操作栏 24px、SKU 行 20px，19 个名称均无溢出；证据合并记录在 `v1.1.26` 三主题截图与 `design-qa.md`。

## 45. 白天、蓝调与夜晚主题切换

- [x] 45.1 根据用户截图同步 proposal、design、商品图采集增量规格和 README，定义点击品牌区循环主题、平台后当前主题图标、完整界面配色覆盖及原地状态保持。
- [x] 45.2 增加失败回归断言，覆盖主题按钮、固定循环顺序、三枚图标、无障碍提示、三套变量和不调用 `render()` 的原地切换。
  - 实现前聚焦用例按预期失败，首个失败点为旧面板没有 `PANEL_THEMES` 三主题定义。
- [x] 45.3 实现三主题与当前页面会话内记忆，并把 Manifest、入口、面板、Native host、ZIP 名称和相关测试统一更新到 `v1.1.26`。
  - 实现后面板语法检查、主题聚焦回归和变更级 OpenSpec 严格校验通过。
- [x] 45.4 运行商品图聚焦测试、全量测试、`public/lib` 同步检查、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查，并记录最终 ZIP。
  - 商品图聚焦测试 `67/67`、全量测试 `1508/1508`、`public/lib` 同步检查 `88/88` 和 OpenSpec 严格全项目校验 `23/23` 通过；扩展与 Pages 构建成功。最终 ZIP 为 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.26.zip`（70,432 字节，SHA-256 `7B07EFA0B3F87201EEA66F235B341EE0625B56A16598CCB35A26233963E728CA`）。
- [x] 45.5 在真实商品页逐一复核白天、蓝调和夜晚主题、切换状态保持、平台后图标、文本对比、卡片尺寸与无溢出，并保存设计 QA 截图。
  - 真实 Chrome 1688 商品 `1061922615487` 中，当前工作区面板按白天、蓝调、夜晚、白天固定顺序切换，并分别显示太阳、水滴和月亮；31 张选择、`scrollTop = 1073.33px`、首卡节点和已打开查看器均保持。关闭前选择非默认蓝调后重新打开仍恢复蓝调。三主题文本对比、约 `114.67px` 媒体区、24px 操作栏、20px SKU 行及 19 个 SKU 名称无溢出；日间、蓝调、夜晚和夜晚查看器截图已保存到 `artifacts/design-qa/`。该验收为真实商品页动态加载当前工作区代码，安装态仍需用户在扩展管理页手动重新加载 `v1.1.26` 解压目录。

## 46. 蓝调时刻配色修正

- [x] 46.1 根据用户对真实页面蓝调主题的反馈，同步 proposal、design 和商品图采集增量规格，定义中深海军蓝表面、冷白文字、亮青色选择态、橙色主操作和与白天/夜晚可见区分的配色契约。
- [x] 46.2 增加失败回归断言，覆盖新的蓝调面板、表面、分组、选择、SKU、主操作和查看器变量，以及 `v1.1.27` 版本链路。
  - 实现前专项用例准确命中旧蓝调的 `#e5eff9` 面板和 `#edf6ff` 表面；实现后蓝调与版本专项测试 `34/34` 通过，并继续明确拒绝旧浅色变量。
- [x] 46.3 只替换蓝调变量并更新 Manifest、入口、面板、Native host、ZIP 名称和相关测试到 `v1.1.27`；白天、夜晚、主题顺序和原地状态保持逻辑不变。
  - 蓝调面板/表面改为 `#1e3a5f / #294b73`，三组背景为 `#2a527e / #25496f / #203f61`，选择态为 `#4de0d2`，SKU 行为 `#155b66 / #c5fff8 / #53bdb5`，主按钮为 `#c5532d`；版本链统一为 `v1.1.27`。
- [x] 46.4 运行商品图聚焦测试、全量测试、`public/lib` 同步检查、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查，并记录最终 ZIP。
  - 商品图相关聚焦测试 `68/68`、全量测试 `1509/1509`、`public/lib` 同步检查 `88/88` 和 OpenSpec 严格全项目校验 `23/23` 通过；扩展与 Pages 构建成功，`git diff --check` 无空白错误，19 个相关文件通过严格 UTF-8 与替换字符检查。
  - 最终 ZIP 为 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.27.zip`（70,550 字节，SHA-256 `09DB95FBE5FDA6E08CE805D559E71EA07780AC5556E96AC1F9A96E372737EDBD`）。
- [x] 46.5 在真实商品页复核蓝调时刻的标题栏、选择工具、状态行、三组表面、选中边框、SKU 名称、页脚和查看器，确认与白天/夜晚有明显区别、文字无溢出且切换状态保持，并保存新版截图。
  - 真实 Chrome 1688 商品 `1061922615487` 本次采集 24 张图片，其中 5 张主图、19 张 SKU 图；详情接口不可用时明确显示部分结果提示。蓝调、夜晚、白天面板背景分别为 `rgb(30, 58, 95)`、`rgb(21, 26, 32)`、`rgb(246, 247, 249)`。
  - 主题循环期间 24 张选择、`scrollTop = 350px`、首卡节点和已打开查看器均保持；蓝调查看器图片加载成功，六个工具按钮、关闭按钮和两个导航按钮正常。新版截图保存为 `artifacts/design-qa/product-image-panel-v1.1.27-blue-hour.png` 和 `product-image-panel-v1.1.27-blue-hour-viewer.png`，可见文字无重叠或溢出。

## 47. SKU 名称可读字号、完整换行与三主题高对比

- [x] 47.1 根据用户截图同步 proposal、design 与商品图采集增量规格，删除 SKU 单行动态缩字契约，定义固定 `12px` 粗体、完整换行、内容自适应高度、操作栏底部对齐和三主题高对比配色。
- [x] 47.2 增加失败回归断言，覆盖名称行布局、固定字号与字重、正常换行、任意断行、三主题变量，以及动态缩字常量、函数、调度和 resize 监听全部移除。
  - 实现前 SKU 名称专项用例按预期失败，首个失败点为白天主题仍使用旧浅青变量；失败输出同时确认旧面板仍包含 `10px` 最大字号、`1px` 最小字号和动态测量常量。
- [x] 47.3 实现 SKU 名称布局与配色修复，并把 Manifest、入口、面板、Native host、ZIP 名称和相关测试统一更新到 `v1.1.28`。
  - SKU 名称改为 `12px/700/16px`、正常换行和任意断行；动态缩字常量、四个函数、RAF 状态和 resize 监听均已删除。白天、蓝调、夜晚分别使用黄橙、亮青和洋红高对比变量。
  - 面板语法检查与 SKU 专项用例通过；商品图扩展、包、API 和导入控制器聚焦范围 `44/44` 通过，公共模块同步检查 `88/88` 通过。
- [x] 47.4 根据用户追加的真实页面截图，增加短名称不预留多行空白的回归断言，并把名称轨道改为内容自适应；长名称完整换行、固定字号、三主题配色和同排操作栏对齐保持不变。
  - 名称轨道已从 `minmax(56px, 1fr)` 改为 `auto`；旧最小高度断言先按预期失败，更新后的面板专项测试 `30/30` 与变更级 OpenSpec 严格校验通过。
- [x] 47.5 运行商品图聚焦测试、全量测试、`public/lib` 同步检查、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查，并记录最终 ZIP。
  - 命名专项测试 `9/9`、商品图聚焦范围 `44/44`、最终面板专项测试 `30/30`、全量测试 `1510/1510`、`public/lib` 同步检查 `88/88` 和 OpenSpec 严格全项目校验 `23/23` 通过；扩展与 Pages 构建成功，`git diff --check` 无空白错误，31 个修改文件通过严格 UTF-8 与中文替换字符检查。
  - 最终 ZIP 为 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.28.zip`（70,078 字节，SHA-256 `6FF7B93D79F83381FE092C9E6E7B2151B6BF03E48780A477E780BAA75C00198E`）。
- [x] 47.6 在真实商品页逐一复核白天、蓝调和夜晚主题，确认短名称高度自然、最长 SKU 名称以固定可读字号完整显示、无截断、无重叠，卡片操作栏底部对齐，并保存设计 QA 截图。
  - 真实 Chrome 1688 商品 `1051486927553` 的 3 个短 SKU 名称行均约 `24.67px`，不再保留旧 `56px` 下限；三张卡片操作栏底边均为 `841.93px`。白天、蓝调、夜晚名称行前景/背景对比度分别为 `10.95:1`、`8.23:1`、`11.31:1`。
  - 临时长名称样例在固定 `12px` 下自然增长到约 `72.67px`，`clientHeight = scrollHeight = 72px`、`clientWidth = scrollWidth = 119px`，同排操作栏底边均为 `899.58px`；验收后已还原真实名称、白天主题和原滚动状态。三主题、自然高度、长名称及同框证据保存在 `artifacts/design-qa/`。

## 48. Studio 与采集窗图片四边留白

- [x] 48.1 根据用户提供的 Studio 批次弹窗和扩展采集窗截图，同步 proposal、design 与两份增量规格，定义图片本体为媒体区宽高的 98%，四边各保留约 1% 纯白背景，卡片及交互布局保持不变。
- [x] 48.2 增加失败回归断言，分别覆盖 Studio 导入缩略图和扩展采集卡片图片的 `98% × 98%` 尺寸与 `1%` 外边距。
  - 实现前聚焦范围 `29/31` 通过，两个失败分别准确命中 Studio 导入缩略图和扩展采集卡片仍使用 `100% × 100%`。
- [x] 48.3 实现两处图片留白，并把 Manifest、入口、面板、Native host、ZIP 名称和相关测试统一更新到 `v1.1.29`。
- [x] 48.4 运行商品图聚焦测试、全量测试、`public/lib` 同步检查、扩展与 Pages 构建、OpenSpec 严格校验、差异和中文编码检查，并记录最终 ZIP。
  - 聚焦回归 `221/221`、全量测试 `1510/1510`、`public/lib` 同步检查 `88/88` 和 OpenSpec 严格全项目校验 `23/23` 通过；扩展与 Pages 构建、发布一致性检查成功，生产依赖审计为 0 个漏洞，差异及 5 个本轮 OpenSpec 文件的严格 UTF-8 检查通过。
  - 最终 ZIP 为 `artifacts/extensions/GPT-Image2-Studio-Product-Image-Collector-v1.1.29.zip`（70,087 字节，SHA-256 `4161AEB5B563AB127A1E7A89B8147FB8D5AEEA68509E6E76DD3065E8382F2341`）。
- [x] 48.5 在桌面和窄视口复核两处卡片图片四边白边、卡片尺寸、覆层、复选框和操作栏均保持稳定，并保存视觉验收结果。
  - Studio `1440×900` 下预览区约 `210.89px`、图片约 `206.67px`、四边约 `2.10–2.11px`；`390×844` 下图片占预览区约 `97.996%`、四边约 `1.53–1.54px`，候选区 `clientWidth = scrollWidth = 355px`。
  - 扩展 `900×900` 下媒体区约 `114.67px`、图片约 `112.36px`、四边约 `1.15px`；`480×844` 下图片占媒体区约 `97.996%`、四边约 `2.19–2.21px`，信息条与复选框均位于媒体区内、操作栏保持 24px，分组区 `clientWidth = scrollWidth = 469px`。
  - 四张验收图保存为 `artifacts/design-qa/product-image-inset-v1.1.29-studio-wide.png`、`product-image-inset-v1.1.29-studio-narrow.png`、`product-image-inset-v1.1.29-panel-wide.png` 和 `product-image-inset-v1.1.29-panel-narrow.png`；两处验收控制台 `warn/error` 为 0。
