## 1. 回归测试

- [x] 1.1 增加自动应用后用途下拉框仍可编辑、状态一次完成和无需第二次点击的浏览器断言。
- [x] 1.2 增加 33 项大冻结计划通过、超过 4 MiB 拒绝和必填字段校验回归。
- [x] 1.3 增加活动结果卡不渲染长覆盖文本、详情数据仍保留的静态断言。

## 2. 实现

- [x] 2.1 将参考图识别应用收敛为最终状态事务并保留明确反馈。
- [x] 2.2 移除识别后的用途锁定，保证手动组别选择立即进入 payload。
- [x] 2.3 调整冻结计划大小边界并保持本地/Worker 共享解析。
- [x] 2.4 隐藏活动结果卡下方长覆盖文本，保留记录与详情字段。

## 3. 验证

- [x] 3.1 运行定向 Creation 测试和完整 `npm test`。定向测试 295/295 通过；完整测试 1280/1281 通过，剩余失败为既有文章插图 lightbox 静态断言漂移，与本变更无关。
- [x] 3.2 运行 `npm run sync:public-lib -- --check`、Pages 构建和 OpenSpec 严格校验。
- [x] 3.3 扫描新增中文内容的 UTF-8 编码与替换字符。
