## 1. 规格与实现准备

- [x] 1.1 确认现有提示词加载细流在循环首尾同时归零，并记录无缝循环验收条件。
- [x] 1.2 完成 proposal、design 和 creation-mode 增量规格。

## 2. 动效实现

- [x] 2.1 为提示词加载壳增加错相细流轨道，并保持装饰语义和节点复用。
- [x] 2.2 为各阶段计算有界半周期偏移并同步 `lib` 与 `public/lib`。
- [x] 2.3 调整细流关键帧首尾，确保循环边界没有可感知的硬重置。
- [x] 2.4 扩展 reduced-motion 覆盖，确保两条细流均可静态停留。

## 3. 验证

- [x] 3.1 更新预览加载运行时和 CSS 契约测试。
- [x] 3.2 运行聚焦测试、镜像同步检查、`git diff --check` 和 OpenSpec 严格校验。
- [x] 3.3 在桌面和窄视口检查连续循环、容器裁剪和布局稳定性。
