## 1. Prompt Control

- [x] 1.1 在提示词生图参数区增加默认关闭的透明背景开关，并在模型协议通道隐藏该控件。
- [x] 1.2 将透明选择快照到提示词任务，开启时固定 PNG，关闭时恢复此前格式。

## 2. Request Handling

- [x] 2.1 服务端以独立 `imageBackground` 字段归一化背景值，并限制其只作用于提示词任务。
- [x] 2.2 在 Responses 与支持的直连图像请求中透传背景值，默认请求保持不透明语义。
- [x] 2.3 在任务和保存元数据中保留提示词任务实际使用的背景值。

## 3. Verification

- [x] 3.1 覆盖默认不透明、透明请求体和 PNG 约束的单元与集成测试。
- [x] 3.2 执行受影响测试、OpenSpec 严格校验和中文内容检查。
