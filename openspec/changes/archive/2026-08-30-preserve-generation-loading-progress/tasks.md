## 1. 进度源生命周期

- [x] 1.1 为共享加载源增加临时保留与按 key/prefix 显式释放能力。
- [x] 1.2 在套图卡片跨队列切换 key 时保留旧运行任务的进度源。
- [x] 1.3 在套图队列成功、失败或异常结束时释放该队列的进度源。

## 2. 回归验证

- [x] 2.1 覆盖核心加载壳 A -> B -> A 的进度恢复、独立起点和显式释放。
- [x] 2.2 覆盖套图卡片跨队列切换后恢复原进度。
- [x] 2.3 同步浏览器镜像并运行相关测试、OpenSpec 严格校验。
