## 1. 规格与界面

- [x] 1.1 已确认当前 prompt/reference 与共享 lightbox 的 DOM、状态和既有测试边界。
- [x] 1.2 已补充 proposal、design 和两个带 delta 标题的增量规格，覆盖参考图预览上下文、输入清空、结果回收和参数页文件信息。
- [x] 1.3 更新提示词模式标题、预览工具栏和图片详情参数面板。

## 2. 行为实现

- [x] 2.1 接入参考图/提示词独立清空按钮并保持现有状态隔离。
- [x] 2.2 实现点击添加当前预览、原生拖拽 drop、去重/上限反馈和拖入视觉状态。
- [x] 2.3 调整参考图预览遮罩与参数面板文件信息布局；桌面和移动视口均需保留参数标题、字段和值的可辨认上下文。
- [x] 2.4 将两个清空按钮统一为线性垃圾箱图标，将提示词模板入口改为 `⭐`，桌面标题行按钮缩至与左侧文字等高并在触控布局保留 `44×44px` 点击区域；同时调整清空和添加按钮的悬浮说明宽度、换行与窄视口溢出边界，避免中文竖排或被折叠面板裁剪。
- [x] 2.5 实现提示词模式胶片条首次载入 10 张、当前页面提交生成后 50 张且重新载入复位的页面会话窗口。

## 3. 验证

- [x] 3.1 更新并运行前端静态契约和 lightbox 相关测试，覆盖清空按钮隔离、结果回收/拖放反馈、参考图遮罩上下文以及参数页文件字段。
- [x] 3.2 已运行提示词工作台布局测试（173/173）、空闲动效/资源版本测试（4/4）、OpenSpec changes strict validation（4/4）和目标文件 `git diff --check`，均通过；本次未重复运行与该按钮样式无关的完整项目套件。
- [x] 3.3 已在隔离端口检查桌面与移动布局下按钮、拖拽目标和参数面板无重叠。
- [x] 3.4 已在桌面和 390px 移动视口检查清空与添加按钮说明的实际宽度、换行和裁剪边界。
- [x] 3.5 覆盖 10/50 张切换、运行中占位独立计数、非持久化复位和提示词模式隔离，并在隔离端口检查真实胶片条。
