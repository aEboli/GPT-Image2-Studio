## 1. 规格与设计

- [x] 1.1 记录 Prompt Kit 从视口右缘改为主预览中线锚定的行为变更。
- [x] 1.2 明确运行时几何测量、CSS 回退和受限布局覆盖策略。

## 2. 实现

- [x] 2.1 在工作台引用中加入主预览画布，并在高度/尺寸同步路径中发布面板锚点 CSS 变量。
- [x] 2.2 将 desktop/narrow-desktop 的 Prompt Kit 面板改为从预览锚点向右展开，保留 tablet/stacked/mobile 回退。
- [x] 2.3 保持模板表单与关闭、焦点恢复、本地存储行为不变。

## 3. 验证

- [x] 3.1 更新静态布局契约，覆盖预览锚点变量、宽度约束和受限回退。
- [x] 3.2 在隔离服务验证宽桌面、1280px 桌面、窄桌面/受限视口的真实几何，确认参数区无重叠。
- [x] 3.3 运行聚焦测试、OpenSpec 严格校验和 `git diff --check`。
- [x] 3.4 归档变更并确认增量规格已合并到主规格。
