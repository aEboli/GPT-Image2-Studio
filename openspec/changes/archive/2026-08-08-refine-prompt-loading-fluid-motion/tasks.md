## 1. 规格与实现准备

- [x] 1.1 确认截图对应提示词模式的 `preview-loading-shell`，并记录与 Creation 卡片加载壳的边界。
- [x] 1.2 完成 proposal、design 和增量规格，定义重力方向、分层液体和减少动态效果边界。

## 2. 动效实现

- [x] 2.1 为预览 motion 节点增加液面、细流和沉积装饰层，并保持 `aria-hidden` 和节点复用。
- [x] 2.2 为各加载阶段计算有界的视觉重力/黏度参数，并同步 `lib` 与 `public/lib`。
- [x] 2.3 将对称漂浮改为下沉、撞击摊平和黏滞回流；多任务场改为稳定屏幕重力方向的低幅漂移。
- [x] 2.4 扩展减少动态效果样式，确保新层和新关键帧可静态停留。

## 3. 验证

- [x] 3.1 更新预览加载运行时和 CSS 契约测试，覆盖分层结构、参数属性、底部锚定、重力关键帧和 reduced-motion。
- [x] 3.2 运行聚焦测试、`sync:public-lib -- --check`、`git diff --check` 和 OpenSpec 严格校验。
- [x] 3.3 在隔离端口用桌面和窄视口检查液体不越界、中心布局不抖动，并记录真实浏览器截图结果。
