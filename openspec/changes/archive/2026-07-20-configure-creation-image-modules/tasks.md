## 1. 行为测试

- [x] 1.1 增加布局测试，断言三个模块开关结构一致、SKU 默认开启且信息图重构默认关闭
- [x] 1.2 增加计划与队列测试，覆盖 SKU 开关关闭时追加项和计数为零，以及历史缺失字段的兼容默认
- [x] 1.3 增加提交、恢复与修复参数测试，覆盖两个显式布尔值在浏览器、Node 和 Cloudflare 路径中的透传

## 2. 界面与状态实现

- [x] 2.1 添加“SKU 图”模块开关并调整“信息图重构”的 HTML 默认值
- [x] 2.2 在浏览器表单引用、set 归一化、草稿恢复、计划预览和提交参数中接入两个默认值

## 3. 计划与生成实现

- [x] 3.1 在共享计划与队列逻辑中支持 `skuGenerationEnabled`，关闭时移除追加 SKU 项并同步计数
- [x] 3.2 在服务端、Cloudflare、manifest 和修复路径中保存并恢复两个模块开关
- [x] 3.3 同步 `lib` 与 `public/lib` 镜像并确认无漂移

## 4. 验证

- [x] 4.1 运行相关聚焦测试和构建/语法检查
- [x] 4.2 运行 `npm run sync:public-lib -- --check` 与 OpenSpec 严格校验
- [x] 4.3 在桌面和窄视口真实界面中确认三个模块开关可见、无重叠且默认状态正确
