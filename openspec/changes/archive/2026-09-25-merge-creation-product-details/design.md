# 设计

创建表单仅保留原商品描述文本框。提交时将输入原文作为 `productDescription`，并将同一原文传给现有尺寸规格提取流程；卖点继续由商品描述事实分配逻辑识别，不再要求单独输入。

复用历史记录时，将 `productDescription`、`sellingPoints` 和 `dimensionSpecs` 的非空内容按原顺序换行拼接到同一文本框。生成结果和持久化结构仍由现有计划器决定，因此无需迁移已有清单。
