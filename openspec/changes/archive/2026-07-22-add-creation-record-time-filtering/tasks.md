## 1. 基线与失败测试

- [x] 1.1 核对当前 Creation record 规格、活动变更、关键词筛选、60 条渲染上限、筛选删除和画廊日期语义。
- [x] 1.2 增加 Creation record 日期 helper 失败测试，覆盖全部、今天、近 7 天、更早、精确本地日期、无效/未来时间和选项计数。
- [x] 1.3 扩展删除 helper 失败测试，覆盖纯时间筛选、组合筛选摘要和默认筛选不能删除全部。
- [x] 1.4 增加浏览器静态失败测试，覆盖时间控件、组合筛选、完整结果集、计数、清空操作和响应式布局。

## 2. 筛选实现

- [x] 2.1 新增 `creation-record-filter` 纯函数模块并同步 `public/lib`，按 `createdAt` 的本地自然日计算结果。
- [x] 2.2 增加套图记录快捷时间、精确日期和清空筛选控件及状态，保持控件互斥与无障碍状态同步。
- [x] 2.3 将关键词、时间、计数、空状态、详情选择和最多 60 条显示接入同一完整组合筛选管线。
- [x] 2.4 让筛选删除识别显式关键词或时间条件，使用完整组合结果并在确认框展示筛选摘要。
- [x] 2.5 补齐桌面、平板和手机筛选栏样式，保持稳定宽度和触控目标。
- [x] 2.6 更新 README 的套图记录能力说明。

## 3. 验证与归档

- [x] 3.1 运行日期 helper、删除 helper和记录页静态聚焦测试。
- [x] 3.2 运行完整 `npm test`、`npm run sync:public-lib -- --check` 与 `npm run build:pages`。
- [x] 3.3 运行 OpenSpec strict validation、中文 UTF-8/乱码扫描和 `git diff --check`。
- [x] 3.4 使用隔离记录在真实浏览器验证快捷时间、精确日期、关键词组合、清空筛选、筛选删除和移动端布局。
- [x] 3.5 将增量规格合并回 `openspec/specs/creation-mode/spec.md`，归档变更并复验主规格。
