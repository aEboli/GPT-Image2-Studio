## 1. 调度器

- [x] 1.1 在 `lib/creation-reference-labels.mjs` 抽取统一的候选评分、去重和预算裁剪函数，主体锚点始终优先。
- [x] 1.2 用角色相关度、coverage 来源和角色计数形成的信息多样性偏好排序候选，移除 `maxSupporting`/固定三张的按类型数量逻辑。
- [x] 1.3 增加单项总图片数、累计原始字节和有限单项硬上限常量；保留 `MAX_CREATION_REFERENCE_IMAGES` 全局上传校验。
- [x] 1.4 对未知字节长度、重复文件、主体锚点超预算和候选不足实现确定性处理，并返回去重后的选择结果（不新增摘要 API）。

## 2. 服务端接入

- [x] 2.1 让 coverage、角色过滤和兜底分支全部调用同一调度器，禁止回退为附加整批上传图。
- [x] 2.2 确认套图首次生成与补图传入相同元数据时使用完全一致的参考图集合与顺序。
- [x] 2.3 保持信息图重构、SKU 精确来源匹配和无角色元数据时主体锚点兜底行为不变。

## 3. 测试

- [x] 3.1 增加同角色多图可超过三张、相关度排序、信息多样性和重复去重测试。
- [x] 3.2 增加字节预算、动态数量预算、硬上限和主体锚点超预算测试。
- [x] 3.3 增加 coverage、角色过滤、元数据缺失和补图一致性测试。
- [x] 3.4 保留并验证全局最多 15 张套图参考图上传限制及信息图/SKU 回归测试。

## 4. 验证与文档

- [x] 4.1 更新相关服务端静态契约，确认调度结果使用裁剪后的选择集合且日志（如有）不含图片内容。
- [x] 4.2 运行 `node --check`、聚焦测试、`git diff --check` 和 `npx openspec validate adaptive-creation-reference-scheduling --strict`。
