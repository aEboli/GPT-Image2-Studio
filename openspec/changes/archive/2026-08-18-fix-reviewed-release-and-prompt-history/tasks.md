## 1. 发行回归测试

- [x] 1.1 增加补丁更新在暂存失败和部分提交失败时恢复全部原始文件、且不遗留新发行说明的测试。
- [x] 1.2 增加文件专属当前版本锚点测试，证明历史版本、示例、标签命令和相似版本字符串不会被修改，零匹配或多匹配会在写入前失败。
- [x] 1.3 增加工作台版本元素位于注释/模板、被隐藏、重复或版本不一致时发行检查失败的测试，并保留唯一有效元素的通过用例。
- [x] 1.4 增加暂存临时文件、回滚临时文件和提交后备份清理失败的错误传播测试，区分未提交与已提交结果。

## 2. 发行维护实现

- [x] 2.1 将补丁更新拆为无副作用的更新计划与全量预检，为 JSON、README、Windows 文档和工作台版本元素使用专属锚点。
- [x] 2.2 使用同目录暂存、原始备份、受控提交和逆序回滚实现整组文件更新，清理成功与失败路径的临时产物。
- [x] 2.3 让补丁更新与发行一致性检查复用版本元素识别语义，只接受唯一、可渲染且与根包版本一致的 `.app-version` DOM 元素。
- [x] 2.4 聚合并报告事务清理失败；提交后仅备份删除失败时保留已提交的新版本并返回待处理备份路径。

## 3. Prompt 历史回归与实现

- [x] 3.1 增加历史存储/API 往返分析模式的测试，并覆盖普通反推、参考图编排及其他非普通分析模式。
- [x] 3.2 增加模板同步资格测试：明确普通模式可导入，明确非普通/未知模式不可导入，缺少模式的旧五组或可识别旧反推结构可兼容，`prompts[]` 和不明确旧记录不可导入。
- [x] 3.3 保存规范化分析模式，并在即时自动保存和长期历史补齐前使用同一普通图片反推资格判断；保持历史展示、已有模板、删除屏蔽和公共模块镜像行为。
- [x] 3.4 移除 `docs/releases/v0.2.7.md` 中易过期的固定测试数量，同时保留完整测试通过、无 `skip` 或 `todo` 的可复核结论。
- [x] 3.5 保留旧历史缺少 mode 的状态，仅规范化显式 mode，并覆盖追加新记录后的文件往返。

## 4. 验证与归档

- [x] 4.1 运行发行维护、Prompt Agent 存储和模板同步定向测试，并检查失败用例未留下临时文件或工作树漂移。
- [x] 4.2 运行完整串行测试、公共模块同步检查、发行一致性检查、Pages 构建、桌面冒烟测试和 `git diff --check`。
- [x] 4.3 运行 OpenSpec 严格校验，合并增量规格并归档本变更，再验证归档后的全部规格。
