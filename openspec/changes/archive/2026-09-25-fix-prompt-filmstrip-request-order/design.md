## Decision

复用运行中预览任务已有的 `createdAt` 排序键，将 `getStablePreviewLoadingItems` 改为时间倒序。该值在任务状态更新时保持不变，因此 SSE 和轮询重绘会保留位置。时间相同的任务沿用输入顺序；任务列表在提交和轮询路径中均按最新优先排列。

不调整缩略图容器的 CSS 定位。当前位置问题来自数据排序方向，不是缺少绝对定位或固定尺寸。

## Verification

- 用单元回归断言较新 `createdAt` 排在较早任务之前，并断言时间相同时输入顺序稳定。
- 用胶片条集成回归确认 15 个任务窗口仍显示最新任务在左。
