## 1. 回归测试

- [x] 1.1 增加组合时间天气、动作道具、主体和环境的名称测试。
- [x] 1.2 覆盖旧标题优先、40 字限制、无后缀文件名回退和固定兜底。
- [x] 1.3 覆盖自动模板文件名式名称迁移与用户自定义名称保留。

## 2. 实现

- [x] 2.1 增加共享图片反推显示名称模块并同步浏览器副本。
- [x] 2.2 让历史记录和新自动模板使用统一显示名称。
- [x] 2.3 让旧自动模板在读取时按兼容规则恢复名称。
- [x] 2.4 复查新增中文内容的编码、语义和后缀清理结果。

## 3. 验证与归档

- [x] 3.1 运行语法检查、针对性测试、完整测试和公共模块同步检查。
- [x] 3.2 运行 `openspec validate --all --strict --no-interactive`。
- [x] 3.3 在真实页面检查历史列表、模板列表、名称输入框和窄视口显示。
- [x] 3.4 验证通过后归档变更并确认增量规格已合并回主规格。
