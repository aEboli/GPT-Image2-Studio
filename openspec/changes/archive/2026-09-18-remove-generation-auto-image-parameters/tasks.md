## 1. 尺寸控件与归一化

- [x] 1.1 从普通生图和 Gemini 模型协议尺寸选项中移除 auto，并让默认值指向具体尺寸。
- [x] 1.2 将历史空值、非法值和 auto 迁移为当前比例/路由的具体默认值。
- [x] 1.3 删除不再使用的分辨率自动适配语言文案。

## 2. 请求与持久化边界

- [x] 2.1 覆盖浏览器配置、缓存、队列重试、任务快照和预览保存路径。
- [x] 2.2 覆盖服务端生成入口、mock、manifest、sidecar 和历史记录恢复路径。
- [x] 2.3 保留非分辨率自动字段的既有行为。

## 3. 验证

- [x] 3.1 运行完整测试和 Node 语法检查。
- [x] 3.2 运行公共模块同步检查、严格 OpenSpec 校验和差异空白检查。
- [x] 3.3 重启本地服务并确认配置接口与工作台加载的是新尺寸选项。
