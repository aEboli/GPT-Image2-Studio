## 1. 规范与分类

- [x] 1.1 增加参考图分类覆盖规则，明确 `feature`、`material`、`usage` 的边界。
- [x] 1.2 更新参考图分析 prompt、JSON schema、前端分类选项和自动归类规则。
- [x] 1.3 补充功能卖点分类和旧角色兼容测试。

## 2. 变体尺寸分组

- [x] 2.1 归一化 `dimension_groups`，保留标签、事实、稳定参考索引和文件名。
- [x] 2.2 按组生成尺寸轮播摘要，组内选择 key specs 并锁定原始字段标签。
- [x] 2.3 按 SKU 主体绑定尺寸组；无可靠绑定时只作为公共规格，禁止复制或交叉组合。
- [x] 2.4 保持扁平 `dimensionSpecs`、历史计划和旧 notes 的兼容回退。
- [x] 2.5 重传历史参考图时按明确绑定重映射尺寸组索引和文件名，多候选不猜测。

## 3. 验证

- [x] 3.1 增加大码/小码、不同颜色和同一尺寸卡多组事实的回归测试。
- [x] 3.2 运行语法检查、聚焦测试、全量相关检查和 OpenSpec 严格校验。
- [x] 3.3 复查新增中文文案编码和提示词输出是否可读。
