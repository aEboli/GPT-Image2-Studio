## 1. 回归测试

- [x] 1.1 增加参考分析提示与 schema 测试，固定关键部件多特征色、共有中性色和不确定颜色边界。
- [x] 1.2 增加 SKU 浏览器载荷测试，固定结构化单位标签在逗号和部件名存在时不被拆分。
- [x] 1.3 增加规划器测试，固定单主体完整标签和多单位标签的最终生图提示。

## 2. 实现

- [x] 2.1 更新参考分析契约，使每个单位标签覆盖全部可靠的可见关键部件色。
- [x] 2.2 在服务端规划和浏览器载荷中贯通 `colorNames` 数组，并保留旧 `colorName` 兼容输出。
- [x] 2.3 更新 SKU 颜色生成指令，使完整标签按目标语言显示且不丢失中性部件色或部件限定词。
- [x] 2.4 同步 `public/lib` 浏览器模块并确认中文内容无乱码。

## 3. 验证与归档

- [x] 3.1 运行 SKU 颜色相关定向测试并确认新回归通过。
- [x] 3.2 串行运行完整测试、Pages 构建、公共模块同步检查和 OpenSpec 严格验证。
- [x] 3.3 检查最终 diff、中文编码与工作区边界，合并增量规格并归档本变更。
