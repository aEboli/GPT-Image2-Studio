## 1. Specification

- [x] 1.1 补充运行中提示词请求按提交时间倒序显示的规范。
- [x] 1.2 记录排序根因：运行中预览项使用了提交时间正序。

## 2. Implementation

- [x] 2.1 将运行中预览任务排序改为最新优先，并保持相同时间戳的输入顺序。
- [x] 2.2 增加排序单元回归和胶片条窗口回归。
- [x] 2.3 更新浏览器模块缓存版本并同步对应公共模块镜像。

## 3. Verification

- [x] 3.1 运行预览加载和胶片条聚焦测试。
  - 本次相关的 3 个单测全部通过；两份聚焦测试文件整体为 218 通过、4 失败，失败项对应现有的缓存版本、公共镜像和界面改动断言。
- [x] 3.2 检查 OpenSpec 严格校验、语法、模块同步和差异空白。
  - 严格 OpenSpec 校验、三个 JS 语法检查、对应公共模块哈希和本次文件的 `git diff --check` 均通过。
  - 全量公共模块同步检查仍报告 `creation-suite-queue.mjs` 与其公共镜像不一致；该文件不是本次改动范围。
