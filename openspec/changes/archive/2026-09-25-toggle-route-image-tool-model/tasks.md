## 配置

- [x] 1.1 本地、浏览器私有、请求私有及所选路由配置新增 `includeImageToolModel`，默认开启，兼容缺少该字段的旧配置。
- [x] 1.2 贯通浏览器配置往返和服务端配置保存接口。
- [x] 1.3 验证默认值、旧配置归一化、关闭状态持久化及请求载荷传递。

## 请求

- [x] 2.1 将所选工具模型和开关贯通全部八个生成入口，包括套图修复与共享生图参数。
- [x] 2.2 默认及开启时发送所选 `tools[].model`，关闭时省略该属性。
- [x] 2.3 验证直连、Gemini、Grok 与图片编辑既有行为不受影响。

## 界面

- [x] 3.1 路由面板新增默认开启的开关，复用直连流式开关样式，仅控制工具模型字段。
- [x] 3.2 桌面端并排显示两个模型，开关横跨下方整行；窄屏纵向排列。
- [x] 3.3 添加中英文文案、无障碍标签及开关状态。
- [x] 3.4 更新布局与交互测试，验证排队任务保留提交时的模型及开关。

## 验证

- [x] 4.1 237 项定向回归测试通过。
- [x] 4.2 8 项相关布局测试通过，122 个浏览器模块同步检查通过。
- [x] 4.3 执行 OpenSpec 严格校验与差异检查；本次修改文件通过空白检查，工作区既有问题与实机验证限制见 `verification.md`。
