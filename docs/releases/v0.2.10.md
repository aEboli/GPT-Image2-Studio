# GPT-Image2-Studio v0.2.10

`v0.2.10` 将主应用版本从 `0.2.9` 更新到 `0.2.10`。

## 更新内容

- 修正 README 中滞留的 v0.2.8 版本事实：徽章、更新说明章节、桌面安装包与免安装 ZIP 文件名、发行说明链接和构建产物路径统一对齐当前版本，并补齐 v0.2.9 的统一生图加载反馈说明。

## 升级说明

- 主应用每次更新仅递增一个补丁版本，即 `+0.0.1`。
- 既有配置、生成记录和图片资产不需要迁移。
- 商品图采集扩展使用独立版本线，本次主应用更新不会修改扩展版本。

## 验证

- `npm test`：1671/1671 通过。
- `npm run sync:public-lib -- --check`：96 个共享浏览器模块一致。
- `npm run check:release`：发布一致性检查通过（v0.2.10）。
- `npx openspec validate --all --strict`：35/35 通过。
- `git diff --check`：无空白问题。
- `npm run build:desktop`：NSIS 安装包与免安装 ZIP 构建成功。
- `npm run test:desktop-smoke`：Electron `43.2.0` 独立窗口冒烟通过并输出截图。
- 未执行：`npm run build:installer`（IExpress 兼容包本次不随 Release 分发）；`npm run check:release:strict`（工作树存在与本次发布无关的未提交改动）。

## 产物校验值

| 文件 | 大小 | SHA-256 |
| --- | --- | --- |
| `GPT-Image2-Studio-Desktop-Setup-v0.2.10-x64.exe` | 243.1 MB | `E92FE3667109786A89DB1961C08ED9752E901D2EB65A6C07F47CD09DC1050FAC` |
| `GPT-Image2-Studio-Portable-v0.2.10-x64.zip` | 303.3 MB | `760EB6902D8CD325E4367C681410FB1EB542551A7A03C0FC530C6E70F58998D2` |
