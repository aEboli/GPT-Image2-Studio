## 1. Contract and regression coverage

- [x] 1.1 Add unit coverage for a fixed before/after comparison item with no prompt, parameter, or file metadata.
- [x] 1.2 Update static UI contracts so either preset image opens the same two-image-only comparison surface.

## 2. Frontend implementation

- [x] 2.1 Build one style transfer preset comparison item with stable source/result semantics and ordering.
- [x] 2.2 Add the dedicated two-image Lightbox DOM and rendering state while preserving overlay focus and close behavior.
- [x] 2.3 Add responsive styling that hides all unrelated Lightbox information and keeps both images available in the same view.

## 3. Verification and closure

- [x] 3.1 Run focused tests and inspect the comparison in desktop and narrow layouts.
- [x] 3.2 Run the project test suite and strict OpenSpec validation, then archive the completed change.
