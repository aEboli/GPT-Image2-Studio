# 新增图片裂变工作台

## Why

现有套图模式偏向单一电商计划，缺少一条从母图素材到多个空间、多个途径和多种内容方向的图片裂变工作链路。

## What Changes

- 新增独立的“图片裂变工作台”创作视图，支持上传母图并补充本轮裂变目标。
- 复用默认文本模型的 Key 和模型进行组图分析，输出痛点、卖点、适用人群、场景和图片计划。
- 通过空间、途径和内容方向选择形成任务矩阵，每个任务都提供纯英文生图提示词，并可使用选定参考图逐张或批量生图。

## Scope

不改变现有套图、融图分析和默认提示词生图流程；图片生成仍走现有 `/api/generate` 接口。
