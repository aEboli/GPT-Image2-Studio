# Design

## Image-led list cards

图片加载后读取其自然像素尺寸，以列表媒体列的实际宽度计算图片高度，并写入卡片 CSS 变量。卡片、媒体和内容区域共享该高度；提示词通过 CSS 多行省略显示，字号调整时重新计算可用行数。图片视图继续使用自然比例，不受列表高度变量影响。

## Return behavior

打开模板预览前保存模板结果区的 `scrollTop`。左侧返回按钮调用专用返回函数，只关闭灯箱并在下一帧恢复滚动位置；右上角关闭按钮仍使用关闭模板库的原有路径。若模板库意外处于隐藏状态，返回函数会重新打开它。
