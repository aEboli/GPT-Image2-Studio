## 1. 规划语义

- [x] 1.1 将功能效果渲染图的角色、场景、买家决策和套图分工提示词改为单一主体的完整功能可视化，并加入对比版式禁令。
- [x] 1.2 将类目提示词和平台图片类型的展示标签、构图策略改为功能效果渲染，同时保留兼容 ID。
- [x] 1.3 更新 Creation Mode 预览说明，使用户在生成前看到单一主体功能渲染的意图。

## 2. 回归保护

- [x] 2.1 扩展规划器测试，覆盖通用、促销、搜索和平台原生提示词的正向要求与对比版式禁令。
- [x] 2.2 更新平台图片类型策略测试，并同步 `public/lib` 镜像。

## 3. 验证与归档

- [x] 3.1 运行聚焦测试、`npm run sync:public-lib -- --check`、完整测试和 `git diff --check`。
- [x] 3.2 运行 OpenSpec change 和全项目严格验证，更新任务状态并归档变更。

验证记录：聚焦回归 316/316 通过，本次涉及的 `public/lib` 镜像与源文件一致，`git diff --check` 和 OpenSpec change/全项目严格验证通过。完整 `npm test` 与全量镜像检查仍由本次范围外的既有工作区改动阻塞：`public/app.js` 为 17,419 行（预算 17,400 行），且 `lib/api-contract.mjs` 尚未同步到 `public/lib/api-contract.mjs`。
