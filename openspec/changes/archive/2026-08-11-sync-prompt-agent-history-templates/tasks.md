## 1. 规格与设计

- [x] 1.1 记录服务端长期历史与浏览器 Prompt Kit 模板的分离原因。
- [x] 1.2 明确稳定 ID、幂等补齐、不覆盖已有模板和不删除历史的语义。

## 2. 实现

- [x] 2.1 增加可测试的历史模板合并辅助函数并同步到 `public/lib`。
- [x] 2.2 在 Prompt Kit 打开和历史加载后执行补齐，并让普通分析即时保存复用同一规则。
- [x] 2.3 保持用户模板编辑、删除、本地存储和旧版 JSON 归一化行为。
- [x] 2.4 为被用户删除的自动模板记录本地屏蔽 ID，避免后续历史刷新复活。

## 3. 验证

- [x] 3.1 运行历史模板合并单测和 Prompt Agent 静态契约测试。
- [x] 3.2 运行项目全量测试、OpenSpec 严格校验和 `git diff --check`。
- [x] 3.3 在隔离端口验证 Prompt Kit 打开后历史记录出现且参数区仍可见。
- [x] 3.4 归档本变更并确认增量规格已合并到主规格。
