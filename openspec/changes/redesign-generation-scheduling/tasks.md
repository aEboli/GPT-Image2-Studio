## 1. 调度参数

- [x] 1.1 将并发数量统一为默认 `20`、范围 `1`–`50`，并同步浏览器与服务端归一化。
- [x] 1.2 将提交间隔统一为默认 `1000` 毫秒、范围 `200`–`5000` 毫秒；无效输入收敛到合法值。
- [x] 1.3 更新配置控件、提示文案、锁定行为和相关边界测试。

## 2. 共享并发与发车控制

- [x] 2.1 新增按 `clientSessionId + generationScope` 复用的发车闸门，并保留短暂空闲状态以避免连续请求丢失节奏。
- [x] 2.2 将发车等待从 `runWithConcurrency` 移到真实上游生成调用之前；五处并发扇出均取得共享许可。
- [x] 2.3 让会话任务槽位在 scope 活跃期间绑定首个请求的并发上限，并覆盖并发请求携带的更大值。
- [x] 2.4 保留中止检查：断开或批次中止的等待者不消耗发车许可，已占用的 slot 正常释放。
- [x] 2.5 增加 launch gate、task slot、server 静态契约和无 worker 节流测试。

## 3. 套图队列与配置快照

- [x] 3.1 调度器在存在任一运行中套图时不启动下一套。
- [x] 3.2 当前套图的 SSE、自动补图和终态结算完成后再调度下一套。
- [x] 3.3 首个套图入队时冻结并发数量和提交间隔；同批后续任务复用该快照，队列清空后删除快照。
- [x] 3.4 自动补图和队列补图显式传递 `autoRepair=1`，手动补图不带该标记。
- [x] 3.5 增加队列串行、补图等待和 FormData 快照测试。

## 4. 套图请求量控制

- [x] 4.1 持久化每个套图条目的 `generationAttemptCount`，并将非法、负数和非有限值归零。
- [x] 4.2 套图生成、Logo 批量和套图补图使用 `createInRunRetryLedger({ maxRetries: 0 })`，不在同一轮立即重排。
- [x] 4.3 自动补图只选择尝试次数小于 `2` 的未完成条目；耗尽条目不再发起自动请求。
- [x] 4.4 在真实上游调用前递增并保存套图条目尝试次数；保留显式手动补图入口。
- [x] 4.5 增加自动补图过滤、持久化归一化和服务端静态契约测试。

## 5. 规范与验证

- [x] 5.1 收窄本变更 proposal、delta specs 和设计说明，使其只描述已实现行为。
- [x] 5.2 同步所有浏览器共享模块并通过 `node scripts/sync-public-lib.mjs --check`。
- [x] 5.3 通过相关 `node --check`、聚焦测试、`git diff --check` 和 `npx openspec validate redesign-generation-scheduling --strict`。

## 6. 已创建任务的临时失败回查

- [x] 6.1 Responses 流式失败事件保留任务 ID，并仅对明确临时容量/限流错误进入原任务 GET 回查。
- [x] 6.2 HTTP 402 错误 body 在带任务 ID 且命中临时容量信号时进入同一 GET-only 回查；无 ID、认证或真实配额失败保持直接报错。
- [x] 6.3 已知任务回查到完成时复用最终图片，回查未完成或失败时不重新 POST；补充本地 mock 回归测试。

未纳入本变更的退避重排、落盘补救、压力推算和写真跨 pass 尝试上限，保留为后续独立变更，避免规范先于实现。
