## 1. 规格与共享解析

- [x] 1.1 将现有 SKU 颜色解析导出为计划器和队列可共享的函数，保留安全识别与本地化边界。
- [x] 1.2 为单主体、分组主体和无可靠颜色的名称结果添加回归覆盖。

## 2. 名称生成

- [x] 2.1 更新计划器 SKU 项名称，在可靠颜色存在时使用颜色名称并保留原有降级顺序。
- [x] 2.2 更新本地队列 SKU 项名称，使用同一解析函数和目标语言。
- [x] 2.3 同步受影响的 `public/lib` 浏览器模块镜像。

## 3. 验证

- [x] 3.1 执行相关计划器、队列和浏览器模块镜像测试：计划器与队列共 158/158 通过；两个受影响的 `public/lib` 模块 SHA-256 与 `lib` 一致。
- [x] 3.2 执行完整项目测试、OpenSpec 严格验证和差异检查：`npm test` 为 1536 通过、1 跳过、2 个既有无关失败（`public/app.js` shell 预算及 `api-contract` 镜像漂移）；本变更与全部主规格严格校验通过，`git diff --check` 通过。
