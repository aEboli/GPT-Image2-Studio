## 1. 规范与回归基线

- [x] 1.1 新增主体一致性 OpenSpec 提案、设计、任务与 `creation-mode` 增量规范。
- [x] 1.2 增加压缩改名、非首位主体、筛掉中间参考图和重复文件名下的稳定角色映射测试。
- [x] 1.3 增加首个实际附件主体锚点、辅助参考图仅限角色和锚点冲突优先级测试。
- [x] 1.4 增加首次生成、套图补图、历史冻结提示词与 `infographic-rebuild` 隔离回归测试。

## 2. 稳定身份映射

- [x] 2.1 在服务端接收的每张套图参考图上赋予并保留一基 `referenceIndex`。
- [x] 2.2 在角色、SKU 主体、coverage 来源和浏览器归一化路径中保留 `index`、`referenceIndex`、`reference_index` 的兼容读取。
- [x] 2.3 让参考图选择和标签构造在稳定索引存在时优先按原始上传索引关联角色，不使用裁剪后下标替代原始顺序。
- [x] 2.4 保留缺失稳定索引的历史文件名/输入顺序兼容回退，不让该回退覆盖有效稳定索引。

## 3. 主体身份锁

- [x] 3.1 让普通轮播项与 SKU 项把选定主体锚点放在当前项实际附件列表首位，并保持其余附件的原始相对上传顺序。
- [x] 3.2 为首张实际附件生成唯一主体身份权威标签；为后续附件生成 supporting-only 角色边界。
- [x] 3.3 在规划提示词和共享运行时参数中加入同一主体身份锁，覆盖首次生成、补图、重生成、修复和历史冻结提示词。
- [x] 3.4 保持主体内容与原语言保护、智能参考图调度、SKU 精确主体匹配和 `infographic-rebuild` source-only 分支不变。
- [x] 3.5 不新增 `input_fidelity` 或其他上游模型保真参数。

## 4. 验证

- [x] 4.1 运行参考图标签、coverage、生成请求构造、服务端静态契约和规划/运行时 prompt 的聚焦测试。
- [x] 4.2 运行 `node --check`、`git diff --check` 和串行全量测试；记录并区分既有基线失败与本次回归（全量 1885 项中 1869 通过，16 项为工作区既有文案/静态版本基线差异）。
- [x] 4.3 运行 `npx openspec validate strengthen-creation-subject-consistency --strict --no-interactive`，并人工复核新增中文文本无乱码。
