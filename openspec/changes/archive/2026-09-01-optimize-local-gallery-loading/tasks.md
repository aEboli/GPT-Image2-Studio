## 1. 首屏和启动器

- [x] 1.1 移除工作台 HTML 的 Google Fonts 请求，并将正文与标题替换为本机字体栈。
- [x] 1.2 以单次 .NET 监听端口快照优化 PowerShell 启动器，并保留健康检查与回退端口行为。
- [x] 1.3 增加或更新首屏无外网字体和启动器端口探测的回归测试。

## 2. 本地缩略图链路

- [x] 2.1 加入可分发的 JavaScript/Wasm 图像缩放依赖，并实现路径安全、去重的 WebP 缩略图生成器。
- [x] 2.2 让新保存图片后台生成缩略图，删除与重命名流程同步管理派生文件。
- [x] 2.3 为旧图片添加按需同源缩略图路由，验证路径且在不可解码时回退原图。
- [x] 2.4 让画廊 API 返回不同的完整图与缩略图 URL，并覆盖真实缩略图响应。

## 3. 前端消费与验证

- [x] 3.1 让最近输出、胶片条和画廊瓦片显式使用缩略图，保留预览、灯箱和下载的原图行为。
- [x] 3.2 调整浏览器缓存合并优先级，避免旧的完整 data URL 覆盖服务端缩略图。
- [x] 3.3 运行定向单元/集成测试、OpenSpec 严格校验、静态检查和桌面/浏览器本地复测。
