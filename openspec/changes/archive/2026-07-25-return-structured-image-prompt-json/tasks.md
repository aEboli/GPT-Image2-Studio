## 1. 回归测试

- [x] 1.1 增加普通反推严格 schema 与指令测试，覆盖五组顶层字段、主体细节、景别控制、单一建议焦段和禁止重复边界。
- [x] 1.2 增加新结构响应归一化测试，同时证明旧 `prompt` 结果和其他分析模式继续兼容。
- [x] 1.3 增加前端主结果、复制、历史映射、自动模板和新旧模板兼容测试。
- [x] 1.4 更新本地与 Cloudflare 路由测试样例，证明普通反推在不同协议下返回同一五组 JSON。

## 2. 实现

- [x] 2.1 将 `lib/prompt-agent.mjs` 的普通反推指令与严格 schema 改为 `subject`、`framing`、`scene`、`visual`、`avoid`。
- [x] 2.2 增加五组 JSON 专用归一化，并保持 Creation、写真、多参考图编排和旧响应兼容。
- [x] 2.3 将前端展示、复制、历史映射与自动模板统一为格式化 JSON，并移除重复复制动作。
- [x] 2.4 复查新增和修改的中文内容，确认无乱码、异常字符或旧文案残留。

## 3. 验证与归档

- [x] 3.1 运行针对性测试、语法检查和 `npm run sync:public-lib -- --check`。
- [x] 3.2 运行串行完整测试与 `openspec validate --all --strict --no-interactive`。
- [x] 3.3 启动当前代码，用用户指定图片实测接口和页面，检查 JSON 结构、重复、景别参数和响应式布局。
- [x] 3.4 归档 `return-structured-image-prompt-json`，确认增量规格合并至主规格并保留变更历史。
