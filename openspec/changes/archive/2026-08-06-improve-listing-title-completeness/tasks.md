## 1. 复现与规格

- [x] 1.1 固定平台建议下限、证据丰富标题和低硬上限平台的短标题用例。
- [x] 1.2 固定队列 SKU 无颜色、有颜色和浏览器规范化的货号隔离边界。

## 2. Listing 标题补全

- [x] 2.1 在现有安全清理后收集同语言候选并按去重、字符和 UTF-8 字节限制补全短标题。
- [x] 2.2 保持无安全候选时的短标题完成语义，不增加请求或回退。
- [x] 2.3 增加英文 120 字符、低硬上限和中英文货号隔离回归测试。

## 3. SKU 文件命名链路

- [x] 3.1 让队列 SKU 使用 planner 同构的序号/可靠颜色 `filenameToken`。
- [x] 3.2 让浏览器规范化、修复和生成提交保留 `filenameToken`。
- [x] 3.3 增加队列、浏览器状态和本地/Worker 文件名静态回归测试。

## 4. 验证与归档

- [x] 4.1 同步 `public/lib` 并运行定向测试、完整测试和构建检查。
- [x] 4.2 运行 OpenSpec strict validation，确认规格、设计、任务和实现一致。
- [x] 4.3 合并增量规格、保留变更历史并归档本变更。
