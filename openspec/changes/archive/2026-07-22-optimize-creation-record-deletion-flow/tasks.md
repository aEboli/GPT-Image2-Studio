## 1. 基线与失败测试

- [x] 1.1 核对当前 Creation record 规格、删除接口响应、本地状态清理、完整列表加载和手动刷新流程。
- [x] 1.2 增加删除后详情选择失败测试，覆盖保留当前、中间项选下一条、末项选上一条、批量删除和无剩余记录。
- [x] 1.3 更新记录页静态失败测试，要求成功删除流程不调用 `loadCreationSets()`，同时保留手动刷新入口。

## 2. 删除交互优化

- [x] 2.1 在 `creation-record-delete` 增加删除后相邻详情选择纯函数并同步 `public/lib`。
- [x] 2.2 删除请求前保存完整筛选顺序和当前详情 ID，成功后一次性清理列表、勾选、队列、当前工作集和灯箱状态。
- [x] 2.3 移除成功路径的自动完整列表请求与重复中间渲染，只保留一次最终渲染和准确反馈。
- [x] 2.4 保持手动刷新、筛选条件、未删除选择和列表滚动上下文不变。

## 3. 验证与归档

- [x] 3.1 运行删除 helper 和记录页静态聚焦测试。
- [x] 3.2 运行完整 `npm test`、`npm run sync:public-lib -- --check` 与 `npm run build:pages`。
- [x] 3.3 运行 OpenSpec strict validation、中文 UTF-8/乱码扫描和 `git diff --check`。
- [x] 3.4 使用隔离记录在真实浏览器验证无自动列表 GET、相邻选择、筛选保留、滚动保留和三种删除入口兼容性。
- [x] 3.5 将增量规格合并回 `openspec/specs/creation-mode/spec.md`，归档变更并复验主规格。
