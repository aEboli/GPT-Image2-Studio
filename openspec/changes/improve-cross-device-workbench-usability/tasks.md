## 1. 响应式基线与自动化护栏

- [x] 1.1 在 `test/studio-density.test.mjs` 为 `320×568`、`390×844`、`768×1024`、`1024×768`、`1366×768`、`1920×1080` 补充布局分类测试，覆盖平板方向、可用空间、粗指针和桌面兼容行为，并先记录当前失败结果。
- [x] 1.2 在 `test/studio-preview-layout.test.mjs` 增加手机操作优先、平板竖屏单栏、核心 `44px × 44px` 触控目标、页面级无横向溢出、局部滚动及弹层动态视口/关闭入口的契约断言。
- [x] 1.3 增加或扩展浏览器状态测试，证明 resize 和旋转只更新根布局属性，不重建表单、上传、队列、预览、Gallery 选择或已打开弹层节点。

## 2. 视口分类与状态连续性

- [x] 2.1 更新 `lib/studio-density.mjs` 的布局判定，使手机保持单栏、`768×1024` 平板竖屏进入单栏、`1024×768` 平板横屏仅在两侧均满足最小可用宽度时进入双栏，并保持 `1366×768` 与 `1920×1080` 的桌面布局行为。
- [x] 2.2 使用现有同步脚本更新 `public/lib/studio-density.mjs`，运行 `npm run sync:public-lib -- --check` 并确认源码与浏览器副本一致。
- [x] 2.3 在 `public/app.js` 的现有 resize/`visualViewport` 同步路径中写入布局所需的方向、输入能力和可视高度属性或 CSS 变量，确保更新过程不清空或复制任何业务状态。

## 3. 工作台布局与触控适配

- [x] 3.1 调整 `public/styles.css` 的共享手机网格，使设置、必填输入和主要动作先于预览/结果，且视觉顺序与 `public/index.html` 的逻辑与键盘顺序一致。
- [x] 3.2 为平板竖屏建立单栏规则，并为平板横屏建立满足面板最小宽度才启用的自适应双栏规则；验证 Prompt、Style Transfer、Reference Analysis、Image Decomposition、Image Edit、Quick Blend 的设置与预览区。
- [x] 3.3 将同一布局契约应用到 Creation、Portrait、Article Illustration、PPT 及其记录/结果工作区，保持现有业务控件和数据绑定不变。
- [x] 3.4 为手机和粗指针平板的导航、主要表单控件、生成/取消/关闭、预览、Gallery、记录和 Lightbox 核心操作设置至少 `44px × 44px` 的有效命中区域，并确认桌面密度未改变。
- [x] 3.5 修正共享网格轨道、长文本、固定宽度和可变内容容器的溢出来源，确保根页面无横向滚动，同时保留缩略图、标签、队列和记录选择条的局部横向滚动。

## 4. 弹层、Lightbox 与动态视口

- [x] 4.1 统一配置抽屉、Prompt Agent、参考图预览、提示词模板、写真资产面板、PPT 编辑器和其他通用弹层的动态可视高度、安全区、`minmax(0, 1fr)` 内容区与内部滚动规则。
- [x] 4.2 调整 Lightbox 在手机和平板上的图片舞台、查看工具、元数据和既有详情操作布局，确保操作可换行或局部滚动、关闭入口持续可达且不改变现有缩放/下载/删除/复制行为。
- [x] 4.3 使用软键盘或等效 `visualViewport` 高度缩减路径验证聚焦字段、内部滚动和关闭动作均保持可达，并补充发现问题对应的最小样式或同步修复。

## 5. 验证与交付

- [x] 5.1 运行 `node --test test/studio-density.test.mjs test/studio-preview-layout.test.mjs`、`npm run sync:public-lib -- --check` 和 `npm test`，修复仅由本 change 引入的失败并记录结果。
- [x] 5.2 启动真实应用，在 `320×568`、`390×844`、`768×1024`、`1024×768`、`1366×768`、`1920×1080` 检查导航、标准生成模式、Creation、Gallery/记录和至少一个弹层，确认无重叠、无非预期裁切、无页面级横向溢出且主流程可用。
- [x] 5.3 在已填写表单、已选择上传、已有预览或队列状态以及已打开弹层的情况下执行手机/平板旋转和跨断点 resize，确认状态不丢失、节点不重复且目标布局无需刷新即可生效。
- [x] 5.4 复查全部工作模式在 `390×844` 与 `768×1024` 的核心操作和关闭路径，保存可复核的截图/检查记录，并确认本 change 未修改 API、生成逻辑、存储数据或桌面视觉层级。
- [x] 5.5 检查本 change 修改的中文内容和界面文案没有乱码、异常替换字符或编码漂移，并审阅最终 diff 只包含本变更所需文件与代码行。
