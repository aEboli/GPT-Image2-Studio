## 1. 规格与状态边界

- [x] 1.1 确认当前 10/50 逻辑会在提交时直接回填更早历史，并确定以首次基线加当前页面成功结果替代。
- [x] 1.2 补充 proposal、design 和增量规格，定义 10 张基线、成功后追加、50 张滚动上限、独立运行占位和重载复位。

## 2. 前端实现

- [x] 2.1 在提示词工作台内存状态中记录首次基线、当前页面提交的任务 ID 和成功结果文件名。
- [x] 2.2 让胶片条仅合并基线与当前页面成功结果，并在受控候选集内实施 50 张滚动上限。
- [x] 2.3 在 SSE 保存事件和后台任务轮询完成事件中统一登记当前页面提示词结果，且不改变其他模式。
- [x] 2.4 更新前端静态资源版本参数，避免旧浏览器缓存继续加载旧逻辑。

## 3. 验证

- [x] 3.1 扩展胶片条回归测试，覆盖 10+5=15、不回填旧历史、超过 50 的滚动移除、SSE/轮询登记和重载非持久化边界。
- [x] 3.2 运行目标与完整测试、OpenSpec 严格校验、静态公共库同步检查和 `git diff --check`。
- [x] 3.3 在隔离端口确认初始 10 张和重新载入复位；隔离实例未配置 API Key，未发起真实生成，当前会话逐张增长、50 张滚动上限与完成路径由 3.1 自动化回归覆盖。
