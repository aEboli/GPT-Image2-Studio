## 1. 阶段到色相族

- [x] 1.1 依据既有 `statusStage` 取值定义色相族映射，覆盖排队、准备、连接、生成、取回重试、写入与失败。
- [x] 1.2 提供阶段推导与 item 阶段读取的纯函数，未知阶段按等待/生成回退，不猜测更靠后的阶段。
- [x] 1.3 在渲染时写入 `data-generation-loading-stage` 与 `data-generation-loading-family`，缺省时保留上次已知阶段。

## 2. 双维度取色

- [x] 2.1 注册可插值的色相与饱和度属性，水体颜色由色相族与百分比深浅合成。
- [x] 2.2 为七个色相族定义取值，并让浅色主题以亮度偏移压低亮度保证对比度。
- [x] 2.3 按实测感知亮度校准各族起始亮度，使七个阶段亮度接近（深色主题极差由 0.29 收到 0.07）。
- [x] 2.4 移除等待态自带的灰色取色，交由排队色相族统一负责。
- [x] 2.5 阶段切换时色相与饱和度平滑过渡，且不影响液位上升时序与减少动态效果降级。

## 3. 调用点透传

- [x] 3.1 胶片条与生成缩略图入口透传 `statusStage`。
- [x] 3.2 提示词主预览透传 `placeholderState.stage`，等待态按排队族。
- [x] 3.3 套图卡片与文章插图入口透传各自的状态字段。
- [x] 3.4 同步 `lib` 模块到 `public/lib`。

## 4. 验证

- [x] 4.1 补阶段到色相族映射、缺省与未知阶段回退、item 阶段读取测试。
- [x] 4.2 补 DOM 阶段属性与 CSS 双维度取色契约测试。
- [x] 4.3 用 Electron 探针确认七个色相族颜色互不相同、同族内高百分比更深、浅色主题更暗、无阶段属性回退到生成族。
- [x] 4.4 用探针实测各族感知亮度并据此校准，确认亮度极差收敛到可接受范围。
- [x] 4.5 运行 `npm test`、`sync:public-lib --check`、`git diff --check` 与 OpenSpec 严格校验。
