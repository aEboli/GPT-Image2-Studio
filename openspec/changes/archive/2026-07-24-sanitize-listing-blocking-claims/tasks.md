## 1. 回归覆盖

- [x] 1.1 增加 `compatibility claim` 单次 V1 响应回归，确认旧实现复现截图失败
- [x] 1.2 增加全部阻断声明类别、多语言片段和清理后必要字段为空的回归边界
- [x] 1.3 更新原“高风险声明必须失败”用例，只保留上游、解析和不兼容字段结构的非关键词失败断言

## 2. 通用净化实现

- [x] 2.1 抽取共享旧版 Listing 内容遍历器，并实现基于 `CLAIM_RISK_RULES` 的证据感知多语言净化
- [x] 2.2 在 Platform V1 验收中接入阻断声明净化、必要主体修复和残留断言

## 3. 一致性与闭环

- [x] 3.1 验证共享函数、本地 HTTP 服务和 Cloudflare Worker 对问题响应返回一致成功结果
- [x] 3.2 运行 Listing 聚焦测试、Pages 构建、公共模块同步检查和串行全量测试
- [x] 3.3 运行差异、编码和 OpenSpec 全库严格校验并归档变更
