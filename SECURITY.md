# 安全说明

## 支持范围

安全修复以当前主分支和最新发布版本为优先。旧安装包可能不再单独回补，请先在不覆盖本地配置与创作记录的前提下升级并复现问题。

## 本地服务信任边界

本地 Node 服务默认只监听 `127.0.0.1`，只有回环 socket 与回环 Host 同时成立时，普通浏览器和 Electron 桌面版才不需要认证。非回环 Host 即使通过本机代理到达，也必须提供有效令牌，避免 DNS Rebinding 绕过。

非回环客户端访问页面、静态资源、API 或输出文件时，每个请求都必须提供当前启动令牌。支持以下方式：

- 浏览器 HTTP Basic：仅用于客户端直接连接本服务，或由 TLS 反向代理自身发起登录挑战；用户名固定为 `studio`，密码为启动日志中的远程访问令牌。
- 命令行或代理：`Authorization: Bearer <token>`。
- 兼容请求头：`X-Image-Studio-Token: <token>`。

不要把服务直接暴露到公网。令牌未配置时会在每次启动时随机生成；固定令牌应使用足够长的随机值，并通过环境变量注入。非回环 `HOST` 默认拒绝明文 HTTP 启动；`IMAGE_STUDIO_ALLOW_INSECURE_REMOTE_HTTP=1` 只用于明确接受凭据和资产可被网络监听风险的兼容环境。

推荐让反向代理通过回环地址连接本服务并终止 TLS。后端不会信任 `X-Forwarded-For` 等转发头判断真实客户端，非回环 Host 仍要求有效服务令牌；无令牌的回环代理请求会被拒绝，且不会由后端发起 Basic 登录挑战。反向代理必须负责外部身份认证、访问控制和请求大小限制，并向每个后端请求注入显式配置的固定 Bearer 或 `X-Image-Studio-Token` 令牌。

## 密钥与生成资产

- 不要提交 `.env`、`.local/`、API Key、访问令牌、浏览器存储导出或真实请求日志。
- `output/`、`artifacts/`、`dist/` 和 `test-results/` 可能包含提示词、参考图、生成图片、清单和路径信息，应按敏感业务数据处理。
- 云端部署应使用平台 Secret；公共或共享设备不应长期保存浏览器私有配置。
- 第三方 API 网关会接触密钥、提示词与图片，使用前应独立评估其数据政策和可信度。

## 报告漏洞

请优先通过 GitHub 仓库的 Security Advisory 私下报告。报告中包含受影响版本、复现步骤、影响范围和建议修复方向，但不要附带真实 API Key、令牌、客户图片或其他无法公开的数据。在获得修复与披露安排前，不要创建包含可利用细节的公开 Issue。
